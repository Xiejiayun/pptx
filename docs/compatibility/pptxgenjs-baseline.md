# PptxGenJS 4.x conformance baseline

基线版本：`4.0.1`；允许范围：`^4.0.1`。

WP0 只依赖以下公开表面：

- `new PptxGenJS()`。
- `addSlide()` 以及公开的 slide 创建 API。
- `write({ outputType: 'nodebuffer' })` / `writeFile({ fileName })` 产生标准 PPTX。

验证样例包含文本、形状、主题和 slide relationships。PptxGenJS 4.0.1 生成的真实文件可由 `PptxDocument.open()` 导入、局部修改标题，并由 LibreOffice headless 无修复地打开和导出。

adapter 不读取 `_slides` 等私有字段。后续 peer-range conformance test 会对 `^4.0.1` 的最低版本和当前最新兼容版本运行同一组输出导入用例。

## 原生 API 对等进度

| PptxGenJS 4.x surface | 原生 API | 状态 |
| --- | --- | --- |
| `new PptxGenJS()` | `PptxDocument.create()` | 已支持 |
| presentation `pptx.rtlMode` | `CreatePresentationOptions.rtlMode` / `document.rtlMode` | 已支持 |
| presentation `pptx.theme.headFontFace/bodyFontFace` | `CreatePresentationOptions.theme` / `document.theme` / `ThemeModel.setFonts()` | 已支持 native strict 创建、presentation-direct theme 读取/整组编辑、具体 theme partial 编辑；zero-input native 保留 Aptos，explicit partial 使用 PptxGenJS Calibri fallback |
| presentation `pptx.title` | `CreatePresentationOptions.title` / `document.title` | 已支持 strict string 创建、direct core-property 读取/编辑/empty/clear；native omitted 不写 PptxGenJS 品牌默认值 |
| presentation `pptx.author` | `CreatePresentationOptions.author` / `document.author` | 已支持 strict string 创建、direct creator 读取/编辑/empty/clear；native 保留独立 lastModifiedBy |
| PptxGenJS `author` mirrored `cp:lastModifiedBy`（无独立 public field） | `CreatePresentationOptions.lastModifiedBy` / `document.lastModifiedBy` | 已支持 strict string 创建、direct lastModifiedBy 读取/编辑/empty/clear；native 与 creator 独立 ownership |
| PptxGenJS write-time `dcterms:created`（无独立 public field） | `CreatePresentationOptions.createdAt` / `document.createdAt` | 已支持 strict W3CDTF string创建、typed direct created读取/编辑/repair/clear；native write不自动刷新 |
| PptxGenJS write-time `dcterms:modified`（无独立 public field） | `CreatePresentationOptions.modifiedAt` / `document.modifiedAt` | 已支持 strict W3CDTF string创建、typed direct modified读取/编辑/repair/clear；native write不自动刷新 |
| presentation `pptx.subject` | `CreatePresentationOptions.subject` / `document.subject` | 已支持 strict string 创建、direct subject 读取/编辑/empty/clear；native omitted 为 `undefined` |
| presentation `pptx.company` | `CreatePresentationOptions.company` / `document.company` | 已支持 strict string 创建、direct extended-properties Company 读取/编辑/empty/clear；native omitted 为 `undefined` |
| presentation `pptx.revision` | `CreatePresentationOptions.revision` / `document.revision` | 已支持 strict whole-number string 创建、direct revision 读取/编辑/clear；native/PptxGenJS 默认均为 `'1'` |
| 默认 `LAYOUT_16x9` | `create({ slideSize: '16:9' })` | 已支持 |
| `LAYOUT_4x3` / `LAYOUT_16x10` / `LAYOUT_WIDE` | `create({ slideSize: '4:3' / '16:10' / 'wide' })` | 已支持 |
| `addSlide()` 空白页 | `document.addSlide()` | 已支持 |
| `defineSlideMaster()` / `addSlide({ masterName })` | `document.defineSlideMaster()` / `document.addSlide({ masterName })` / `replaceSlideMaster()` / `deleteSlideMaster()` / `masters` / `layouts` | 已支持 strict named-layout selection、真实 parent-master chain、direct layout/master background、transient margin、slide number、rect/line/text/placeholder/image/chart definition objects、stable semantic wrappers、whole-replace、delete+retarget，以及六类 placeholder owner materialization/population；`masterName` 按 PptxGenJS 拼写但选择 layout name |
| `slide.background` / deprecated `slide.bkgd` | `SlideModel.background` / `PptxDocument.setSlideBackgroundImage()` | 已支持 direct inherited clear、explicit no-fill、sRGB/theme solid+transparency、linear/path gradient、PNG/JPEG/GIF image create/read/edit/clear、duplicate COW、relationship isolation、GC、六格式与 PptxGenJS 合法 solid/transparency/PNG final-state conformance；native explicit none 写合法 `a:noFill` |
| `slide.slideNumber` / master `slideNumber` | `CreatePresentationOptions.firstSlideNumber` / `document.firstSlideNumber` / `SlideModel.slideNumber` / `LayoutModel.slideNumber` / `MasterModel.slideNumber` | 已支持 direct slide/layout/master field create/read/edit/clear、position/size/alignment/RTL/vertical alignment/margin/font/language/bold/italic/sRGB/theme/transparency、master flag、start number、lifecycle cache 同步、六格式、diagnostics 与 PptxGenJS public-output conformance |
| `slide.addShape(ShapeType, options)` | `SlideModel.addShape(type, options)` / `SlideModel.addCustomShape()` / `ShapeModel.presetType` / `ShapeModel.adjustments` / `ShapeModel.customGeometry` / `ShapeModel.fill` / `ShapeModel.line` / `ShapeModel.arrows` / `ShapeModel.shadow` / `ShapeModel.hyperlink` | 已支持 178 个 canonical preset、ordered direct preset adjustments、六种 custom path commands、全部 17 个 custom guide-formula operators、ordered XY/polar handles、ordered connection sites 与 token geometry values、strict solid/no-fill fill、none/solid line 的 color/transparency/width/8 preset dash、六种 begin/end arrow type、outer/inner shadow，以及 URL/internal-slide click hyperlink 的 create/read/edit/clear/duplicate/reopen；native transform 使用 EMU/OOXML angle |
| `slide.hidden` | `SlideModel.hidden` | 已支持 strict boolean create/edit、direct root read/repair、duplicate/move/reopen；native 拒绝 truthy coercion |
| `slide.addNotes(string)` | `SlideModel.addNotes()` / `SlideModel.notes` | 已支持 plain string create/read/edit/empty/clear、lazy native state、notes-master repair、duplicate/move/reopen |
| presentation `addSection({ title, order? })` / `addSlide({ sectionTitle })` | `document.sections` / `addSection()` / section editing commands / `addSlide({ sectionTitle })` | 已支持 strict section extension 创建、detached 读取、ID 寻址编辑与 slide lifecycle 同步；native 修复 unknown title 和 order 0，保留 empty section 与 loose slide |
| `defineLayout()` 自定义尺寸 | `create({ slideSize: { width, height } })` | 已支持 |
| 创建后读取或修改页面尺寸 | `document.slideSize` | 已支持 |
| `slide.addText(string)` 单段文本 | `slide.addText(string, options)` | 已支持 |
| string 中的 CR/LF、多段落与空行 | `addText()` / `ShapeModel.text` | 已支持 |
| 文本框 x/y/w/h、旋转、翻转、名称 | `AddTextOptions` + `inches()` / `degrees()` | 已支持 |
| `TextProps[]` 基础 runs、字体、字号、粗斜体、颜色、soft break | `addRichText()` / `ShapeModel.richText` | 已支持 |
| rich run `underline` boolean、17 种有效 style、独立颜色 | `RichTextRunStyle.underline` | 已支持 |
| rich run `strike` boolean、single/double strike | `RichTextRunStyle.strike` | 已支持 |
| rich run `highlight` sRGB/theme color | `RichTextRunStyle.highlight` | 已支持 |
| rich run `outline` solid sRGB/theme color 与 point size | `RichTextRunStyle.outline` | 已支持 |
| rich run `glow` sRGB/theme color、point size 与 opacity | `RichTextRunStyle.glow` | 已支持 |
| rich run `superscript` / `subscript` / custom `baseline` | `RichTextRunStyle.baseline` | 已支持 |
| rich run `charSpacing` 正/负/fractional point | `RichTextRunStyle.characterSpacing` | 已支持 |
| rich run `transparency` 0–100% | `RichTextRunStyle.transparency` | 已支持 |
| outer/run `lang` 默认、继承与覆盖 | `AddTextOptions.lang` / `RichTextRunStyle.lang` | 已支持 |
| 段落 `align: left/center/right/justify` | `AddTextOptions.align` / `RichTextParagraph.align` | 已支持 |
| 段落 `rtlMode` 默认与逐段覆盖 | `AddTextOptions.rtlMode` / `RichTextParagraph.rtl` | 已支持 |
| 标准/custom bullet、16 种 numbering、startAt、indent | `AddTextOptions.bullet` / `RichTextParagraph.bullet` | 已支持 |
| 段前/段后、exact/multiple line spacing | `AddTextOptions.spacing` / `RichTextParagraph.spacing` | 已支持 |
| 多级列表 `indentLevel` 0–8 | `AddTextOptions.level` / `RichTextParagraph.level` | 已支持 |
| `tabStops`：left/center/right/decimal | `AddTextOptions.tabStops` / `RichTextParagraph.tabStops` | 已支持 |
| 文本框 `margin` scalar/TRBL 与 direct 四边编辑 | `AddTextOptions.margin` / `ShapeModel.textMargins` | 已支持 |
| 文本框 `valign: top/middle/bottom` 与 direct anchor 编辑 | `AddTextOptions.valign` / `ShapeModel.verticalAlignment` | 已支持 |
| 文本框 `wrap: boolean` 与 direct wrapping 编辑 | `AddTextOptions.wrap` / `ShapeModel.textWrap` | 已支持 |
| 文本框 `vert` 七种文本方向与 direct 编辑 | `AddTextOptions.vert` / `ShapeModel.textDirection` | 已支持 |
| 文本框 `fit: none/shrink/resize` 与 direct 编辑 | `AddTextOptions.fit` / `ShapeModel.textFit` | 已支持 |
| 文本框 outer `hyperlink` URL/internal slide/tooltip | `AddTextOptions.hyperlink` / `ShapeModel.hyperlink` | 已支持 plain/rich/placeholder/layout/master/declarative 创建；shape click 与每个非空 run 共用 relationship，run 默认单下划线且显式 underline 优先；`ShapeModel.hyperlink` 仍只编辑 whole-shape click |
| paragraph 左右 margin、first-line/hanging indent | `paragraphMarginLeft` / `paragraphMarginRight` / `paragraphIndent` 与 rich paragraph overrides | 已支持 direct 创建、读取、编辑与清除 |
| `slide.addChart(type, data, options)` | `slide.addChart()` / `document.addChart()` / `ChartModel.definition` / `replaceDefinition()` / `replaceSeries()` / `remove()` | 已支持九种标准类型、兼容组合图、主/次轴、X/Y/bubble vectors、内嵌 XLSX/formula/cache 同步、主要 options、诊断、复制/删除/reopen；modern/external/animation 保留态除外 |
| `slide.addTable()` string / cell-object rows + name/x/y/w/h/colW/rowH/align/border/fill/margin/textDirection/valign | `slide.addTable(rows, options)` | 部分支持：strict rectangular single-paragraph strings、`{ text, options?: { align, border, fill, fit, margin, textDirection, valign } }` objects、table/cell align/border/fill/margin/textDirection/valign、cell-level fit、direct paragraph/cell state、EMU geometry、创建时 scalar/per-axis sizes |
| 已有 table `tblGrid/gridCol@w` | `TableModel.columnWidths` / `TableModel.setColumnWidths()` | 已支持 strict snapshot、direct 无损编辑与 transform width 同步 |
| 已有 table direct `tr@h` | `TableModel.rowHeights` / `TableModel.setRowHeights()` | 已支持 zero 自动行高、direct 无损编辑与条件式 transform height 同步 |
| table/table-cell `textDirection` | `AddTableOptions.textDirection` / `AddTableCellOptions.textDirection` / `TableCell.textDirection` / `TableModel.setCellTextDirection()` | 已支持 table/cell 创建与 cell precedence、direct physical-cell 读取/编辑；table getter/editor 尚未支持 |
| table/table-cell `align: left/center/right/justify` | `AddTableOptions.align` / `AddTableCellOptions.align` / `TableCell.horizontalAlignment` / `TableModel.setCellHorizontalAlignment()` | 已支持 table/cell 单段落创建、direct cell 读取/编辑/清除与 PptxGenJS materialized final-state 对等 |
| table/table-cell `valign: top/middle/bottom` | `AddTableOptions.valign` / `AddTableCellOptions.valign` / `TableCell.verticalAlignment` / `TableModel.setCellVerticalAlignment()` | 已支持 table/cell 创建与 direct cell 编辑；table getter/editor 尚未支持 |
| table/table-cell `margin` scalar/TRBL | `AddTableOptions.margin` / `AddTableCellOptions.margin` / `TableCell.margins` / `TableModel.setCellMargins()` | 已支持 point scalar/TRBL/named table/cell 创建与 direct cell snapshot/edit；table getter/editor 尚未支持 |
| table/table-cell `border` scalar/TRBL | `AddTableOptions.border` / `AddTableCellOptions.border` / `TableCell.borders` / `TableModel.setCellBorders()` | 已支持 scalar/TRBL/named table/cell 创建、direct 四边读取/编辑/清除；table getter/editor 尚未支持 |
| table/table-cell `fill` solid/none/transparency | `AddTableOptions.fill` / `AddTableCellOptions.fill` / `TableCell.fill` / `TableModel.setCellFill()` | 已支持 table/cell 创建、direct cell 读取/编辑/清除；table getter/editor 尚未支持 |
| table-cell bodyPr autofit | `AddTableCellOptions.fit` / `TableCell.textFit` / `TableModel.setCellTextFit()` | 原生 cell 创建与 direct 编辑已支持；PptxGenJS 4.0.1 本身无 table fit API |

## 剩余公开表面审计

审计权威来源是锁定依赖 `pptxgenjs@4.0.1/types/index.d.ts`，并以当前聚合包公开导出和真实 OOXML 行为交叉核对。这里的“对等”指新高层 API 覆盖相同能力，不要求现有 PptxGenJS 调用只替换 import 后原样运行。Deprecated alias 只需由对应 canonical 能力覆盖，不另建兼容 facade。

| PptxGenJS 4.0.1 public surface | 当前原生能力 | 尚缺能力 | 状态 / 顺序 |
| --- | --- | --- | --- |
| `version`、`presLayout`、`AlignH`、`AlignV`、`ChartType`、`OutputType`、`SchemeColor`、`ShapeType`、`PlaceholderType` | package manifest 提供版本，页面尺寸可读写，`CHART_TYPES` / `ChartType` 覆盖九种标准图表，`PRESET_SHAPE_TYPES` / `PresetShapeType` 覆盖 178 个 canonical preset，`PLACEHOLDER_TYPES` / `PlaceholderType` 覆盖 title/body/pic/chart/tbl/media，部分文本/颜色值有 focused native types | 完整稳定的 output types 与其他 runtime helper constants；不要求复刻 PptxGenJS namespace 形状 | 部分支持；统一公开 API 收尾 |
| `stream()`、`write({ outputType, compression })`、`writeFile({ fileName, compression })` | `write(): Uint8Array`、Node `writeFile()`、browser `writeBlob()` / `download()`、path/stream 输入已支持 | Node readable 输出、六种 output type、显式 compression policy、返回值语义和跨 Node/browser conformance | 部分支持；输出阶段 |
| `defineSlideMaster()`、`addSlide({ masterName })` / deprecated `addSlide(masterName)` | strict unique named-layout selection；declarative direct background、transient scalar/TRBL margin、slide number、rect/line/plain-rich text/placeholder/image/chart objects；stable master/layout wrappers；六类 placeholder materialize + text/shape/image/chart/table/media population；whole replace、delete/retarget、六格式、packed Node/browser/types/CLI 和 PptxGenJS 4.0.1 public-output conformance 已覆盖 | 完整 theme text cascade、percentage coordinates、advanced object styles 与更广泛 client certification；deprecated positional `addSlide(masterName)` 不复制，使用 typed options | 已支持主能力；高级文本/样式/client 阶段继续 |
| `tableToSlides(eleId, options)` | 无 DOM table importer | HTML table 解析、CSS/column width 映射、分页、重复 header、master 和附加 image/shape/table/text | 未支持；高级表格阶段 |
| `slide.background` / deprecated `slide.bkgd` | slide/layout/master direct inherited clear、legal no-fill、solid/transparency、linear/path gradient、PNG/JPEG/GIF image、portable source loader、duplicate COW、relationship isolation、GC、六格式、packed Node/browser/types/CLI 与 PptxGenJS 合法 final-state conformance 已覆盖 | `p:bgRef` semantic editing、pattern/group fill、image crop/tile/effects；PptxGenJS empty-`bgPr` 缺陷仅识别为 unsupported，不复制 | direct slide/layout/master background 已支持；高级 fill 待后续 |
| `slide.color` | strict transient sRGB/theme default text color；只对后续 `addText()` / `addRichText()` run 物化，local run color 优先；duplicate/move/delete、六格式、packed Node/browser/types/CLI 与 PptxGenJS 合法 final state 已覆盖 | table 不继承；完整 master/layout/placeholder theme text cascade 和更完整 advanced text 仍待后续专项 | direct transient default 已支持；完整文字继承属于 advanced text |
| `slide.slideNumber` | direct slide/layout/master field、signed Int32 `firstSlideNumber`、position/size/alignment/RTL/valign/margin/text style、master flag、duplicate/move/delete cache sync、declarative named-layout integration、diagnostics、packed Node/browser/types/CLI 与六格式均已覆盖 | percentage position，以及更广泛 PowerPoint/Keynote/Google Slides 认证 | direct owner 与 named-layout integration 已支持；其余并入 advanced/client 阶段 |
| `slide.newAutoPagedSlides` | 无 | 表格分页产生的 slides 结果集合及稳定 identity | 未支持；高级表格阶段 |
| `slide.addShape(shapeName, options)` | 178 个 canonical preset、ordered direct `val` adjustment guides、全部六种 custom geometry path command、multiple subpaths/paths/path flags、全部 17 个 typed guide-formula operators、37 个 built-ins、ordered XY/polar adjustment handles、ordered duplicate-preserving connection sites、arbitrary custom text rectangles，以及 adjustment/shape guides 与 token-referenced point/radius/angle/bounds/site/rectangle-edge values 的 pure/live numeric evaluation；strict solid/no-fill fill、none/solid line 的 sRGB/theme color、0–100 transparency、0–1584pt width、8 preset dash、六种 begin/end arrow type、outer/inner shadow，以及 URL/internal-slide click hyperlink 与 direct tooltip 均可原生 create/read/edit/clear（custom geometry 不支持 clear）/duplicate/reopen；strict inputs、stable identity、rollback、effect/relationship preservation、PptxGenJS public-output conformance 和 packed Node/browser/types 已覆盖 | arrow size、cap/compound/alignment/join editing、advanced line fill/custom dash creation、generic/advanced effects、custom shadow transforms、non-shape shadows、hover、text/image/table/chart/media hyperlink creation、action navigation、path scaling、arc endpoint/resolved bounds、handle dragging、connector snapping/creation、shape text options、percentage positions，以及 gradient/pattern/picture/group advanced shape fills | 部分支持；preset geometry/adjustments、custom paths/formulas/handles/connection sites/text rectangles/evaluator、simple fill/line、arrows、click hyperlink、simple outer/inner shadow 已完成 |
| `slide.addImage(options)` | `PptxDocument.addImage()` 已支持 Node path、HTTP/HTTPS URL、browser-relative URL、strict base64 data URI、`Uint8Array`、`ArrayBuffer`、`Blob`/`File`、Web stream 与 async iterable；PNG/JPEG/GIF signature 与 strict SVG XML detection、optional MIME assertion、AbortSignal、name/alt text、EMU transform、pic placeholder name/identity population，以及通用 contain/cover/source-coordinate crop sizing 已完成；SVG 支持 explicit/browser-Canvas/built-in PNG fallback、canonical two-part create/read、`isSvg`/两侧 part URI、paired replace 与 duplicate clone-on-write；底层 raster/SVG create、live `sourceRectangle` 及 pure `calculateImageSizing()` 均已公开 | external SVG relationship、SVG DOM 编辑、script 执行、SVG external-resource 获取、任意 SVG 栅格化一致性、rounding、transparency、alt-text 编辑、hyperlink、shadow、advanced placeholder style、单图片删除和 media GC | 部分支持；raster 与 SVG create/load/read/replace/sizing/placeholder population 已完成，剩余 image style 与 per-image lifecycle |
| `slide.addMedia(options)` | `PptxDocument` 与 `SlideModel` 的 `addAudio()` / `addVideo()` 已支持 path、strict data URI、bytes/ArrayBuffer、Blob/File、Web stream、async iterable、HTTP/HTTPS external、PNG/JPEG/GIF poster、MIME/extension assertion/inference、name/alt text、EMU transform、media placeholder name/identity population、SHA-256+MIME dedup；live `MediaModel` 具备 stable identity、metadata/settings/transform 编辑、embedded↔external source 与 poster replacement/reset、duplicate COW、move、对象/幻灯片删除和引用感知 GC；click/auto、loop、hide-when-stopped、volume 已具备 native PowerPoint timing create/read/edit/clear/adopt/diagnose，普通动画共享 ID allocator；PptxGenJS 4.0.1 的 4/4 个公开有效 embedded 用例及 legacy import 已覆盖 | online video、remote-fetch embedding、trim/bookmarks、有限重复、narration/cross-slide audio、captions/subtitles、crop/rounding/shadow/hyperlink/advanced placeholder styles、内建转码引擎与广泛客户端认证 | 创建、placeholder population、stable live lifecycle 与 native playback timing 已支持；剩余 advanced media/client coverage |
| `slide.addChart(type, data, options)` | `CHART_TYPES` 九种标准类型、compatible combo、主/次轴、category/X/Y/bubble vectors、embedded XLSX/A1 formula/cache 同步、title/legend/axis/gridline/label/data-table/series/type options、strict semantic read/edit/type conversion、duplicate/delete/GC/diagnostics、六格式与 actual packed Node/browser/CLI 已覆盖 | Office 2016 `cx:*` 创建/语义编辑、external workbook 编辑、chart animations、内建 trendline/error-bar 创建、更多 advanced style/fallback 与广泛客户端认证 | 标准图表创建与语义编辑已支持；advanced/plugin/client coverage 继续 |
| `slide.addText(text, options)` | plain/rich text、核心 run/paragraph/text-box layout、named text-placeholder population 已覆盖；`AddTextOptions.fill` / `line` / `arrows` / `shadow` / `hyperlink` / `shape` / `rectRadius` / `isTextBox` 支持 strict direct simple fill/line/begin-end endpoint/outer-inner shadow、URL/internal-slide outer hyperlink、全部 178 个 canonical preset geometry、roundRect absolute radius 与 direct `txBox` 状态；`RichTextRunStyle.hyperlink` 支持 per-run create/read/edit、outer inheritance/false suppression 与独立 relationship；`RichTextRun.breakLine` 支持 middle/trailing/empty/consecutive 段落拆分、段落属性复制、soft-break/hyperlink 组合与 canonical getter，覆盖 slide/layout/master/declarative/placeholder surfaces | 其余 shape-level style editing、完整 theme text cascade 与 percentage positions | 部分支持；text fill/simple line/arrows/simple shadow/outer/per-run hyperlink/preset geometry/rectRadius/isTextBox/breakLine 已完成 |
| `slide.addTable(rows, options)` | rectangular plain single-paragraph table、基础 geometry/size、cell align/border/fill/margin/direction/fit 和 existing direct edits 已覆盖 | nested/rich/multi-paragraph cell text、font/text styles、hyperlink、colspan/rowspan、row/column CRUD、table-level getters/editors、auto-page/repeated headers、content measurement/layout recomputation | 部分支持；advanced text 后 |
| shared `PositionProps` percentage coordinates and object metadata | 原生 public API 使用明确 EMU，支持 absolute transform、rotation、flip 和部分 object names | percentage coordinate normalization，以及所有新建 object 一致的 name/alt text/hyperlink/shadow/placeholder contracts | 部分支持；随各 object creator 落地 |

### Text shape simple fill

`AddTextOptions.fill` 复用 `ShapeFill`，支持 direct `{ kind: 'none' }` 或带 strict sRGB/theme color 与可选 `0..100` transparency 的 solid fill；透明度量化到 `0.001%`。Omitted、runtime `undefined` 和 explicit none 创建都保持 canonical direct `a:noFill`，solid explicit zero 写 `a:alpha val="100000"`。Plain/rich text、`addPlaceholder()`、title/body placeholder population、layout/master wrapper 和 declarative master text/placeholder objects 共用同一 normalizer/renderer；创建结果立即通过 `ShapeModel.fill` 读取、替换或清除，并覆盖 caller detachment、same-value no-op、stable identity、duplicate/move、outer rollback、六格式 write/reopen、sibling 与 layout-placeholder source isolation。

PptxGenJS 4.0.1 public output 对 omitted fill 写 direct no-fill，对 `{ type: 'none' }` 省略 direct fill choice，对 explicit zero transparency 省略 alpha。Native 保留 explicit none 和 zero 的 direct intent，不复制 falsy collapse；合法 sRGB/theme solid 与非零透明度达到相同 final semantics。Native 还会在任何 package mutation 前拒绝 PptxGenJS-shaped `{ color }`、`type`/deprecated `alpha`、missing color、coercible/out-of-range transparency、unknown/accessor/symbol/class input。Gradient/pattern/picture/group text-fill creation 仍待后续；text outer simple line、arrows、simple shadow、outer/per-run hyperlink、preset geometry、`rectRadius`、`isTextBox` 与 rich-text `breakLine` 已在后续小节支持。

跨 package focused gate 为 5/5，SDK/root 与 adapter suites 分别为 188/188、76/76；最终全量为 1262 passed / 1 skipped，独立 performance 为 1/1（560ms），TypeScript typecheck 与 project build 均通过。Actual 57-file tarball 的 installed Node/declarations/browser/CLI 均返回 `textShapeFills: true`；真实 Chrome validation/console/page/network errors 为 0，installed CLI PowerPoint 2010 validation 为 0 errors / 0 warnings，并通过 raw part read 锁定 sRGB/theme、alpha `75000`、alpha `100000` 与 direct no-fill。

### Text shape simple line

`AddTextOptions.line` 复用 strict `ShapeLine`，仅接受 direct `{ kind: 'none' }`，或带 required sRGB/theme color、可选 `0..100` transparency、`0..1584pt` width 与八种 preset dash 的 `{ kind: 'line', ... }`。Transparency 量化到 `0.001%`，width 量化到 1 EMU；省略 width/dash 物化为 1pt/solid，zero width 与 explicit-zero transparency 保留 direct state。Omitted、runtime `undefined` 和 explicit none 创建保持 canonical direct `a:noFill`。Plain/rich text、`addPlaceholder()`、placeholder population、layout/master wrapper 与 declarative master text/placeholder objects 共用 normalizer/renderer；结果立即通过 live `ShapeModel.line` read/whole-replace/clear，并覆盖 caller detachment、same-value bytes/journal no-op、stable identity、duplicate/move、outer rollback、六格式 write/reopen、sibling 与 placeholder-source isolation。

PptxGenJS 4.0.1 的 omitted/none/empty/missing-color text line 都输出 empty `a:ln`，省略 width/dash 时依赖隐式 1pt/solid，并折叠 width zero 与 transparency zero；nested deprecated `alpha` 生效，`lineDash` 被忽略。Native 保留明确可逆的 no-fill、default width/dash 与 zero direct state，严格拒绝 aliases、missing color、invalid dash/range 和 non-ordinary input。合法 sRGB/theme、非零 transparency、正 width 与全部八种 dash 达到相同 final semantics。Gradient/pattern/picture/group line fill、custom dash 与 cap/compound/alignment/join 仍待后续；text arrows、simple shadow、outer/per-run hyperlink、preset geometry、`rectRadius`、`isTextBox` 与 rich-text `breakLine` 已完成。

`AddTextOptions.arrows` 复用 strict `ShapeArrows`，仅接受 optional `begin` / `end` 的 `none/arrow/diamond/oval/stealth/triangle`。Omitted/runtime `undefined`/empty arrows 创建不写 endpoint，explicit `none` 保留 direct type；plain/rich、placeholder 创建与 population、layout/master wrapper、declarative text/placeholder 共用同一 renderer。Line paint/dash 固定先于 `headEnd`，`headEnd` 先于 `tailEnd`；arrow-only native 创建保留 canonical direct no-fill。创建结果可立即通过 live `ShapeModel.arrows` whole-replace/clear，line 与 endpoint ownership 独立，并覆盖 detached input/snapshot、duplicate、rollback、async declarative detachment、六格式与 reopen。

PptxGenJS 4.0.1 text arrows 对六种合法 begin/end、单端/双端、explicit none 与 solid line + arrows 产生相同 final endpoint semantics。其 omitted line 与 arrow-only/`type:none` 输出不写 no-fill，empty endpoints 与 nested/top-level `lineHead`/`lineTail` 被忽略，非法 token 可原样透传；native 保留 canonical no-fill 并在 mutation 前严格拒绝 alias/empty/invalid token。Actual 57-file tarball 的 Node/declarations/browser/CLI 均返回 `textShapeArrows: true`，真实浏览器错误为 0，PowerPoint 2010 profile 为 0 errors / 0 warnings。

跨 package focused gate 为 5/5，model、SDK、root 与 adapter suites 分别为 189/189、182/182、9/9、77/77；最终全量为 1268 passed / 1 skipped，独立 performance 为 1/1（553ms），两种 TypeScript build 与两套 package build 均通过。Actual 57-file tarball 的 installed Node/declarations/browser/CLI 均返回 `textShapeLines: true`；真实 Chrome immediate/detached/reopen 完全匹配且 console/page/network errors 为 0，installed CLI PowerPoint 2010 validation 为 0 errors / 0 warnings。

### Text shape simple shadow

`AddTextOptions.shadow` 复用 strict `ShapeShadow`，支持 direct outer/inner、sRGB/theme color、量化到 `1/100000` 的 `0..1` opacity、量化到 1 EMU 的 `0..100pt` blur 与 `0..200pt` distance，以及量化到 `1/60000°` 的 `0 <= angle < 360`；仅 outer 接受 boolean `rotateWithShape`。Omitted fields 使用 black、0.75、8pt、270°、4pt 与 outer rotate false，explicit zero 均保留。Plain/rich text、`addPlaceholder()`、placeholder population、layout/master wrapper 与 declarative master text/placeholder 共用同一 renderer；effect list 位于 line/endpoints 后。创建结果立即通过 `ShapeModel.shadow` read/whole-replace/clear，fill/line/arrows/effect ownership 独立，并覆盖 deep detachment、same-value exact no-op、duplicate/move、outer rollback、六格式 write/reopen、stable identity、sibling 与 placeholder-source isolation。

PptxGenJS 4.0.1 omitted shadow 与 `{ type: 'none' }` 都不写 direct effect，合法 outer 的 defaults/custom final semantics 与 native 对等，legacy `offset` 对应 native `distance`。其 runtime zero 会 falsy-fallback 到 defaults，text `rotateWithShape: true` 被忽略，missing/invalid type、hash color 与部分 numeric input 会被 warning-correct/coerce，inner 则产生 mismatched closing tag。Native 不复制这些缺陷：保留 explicit zero，支持 theme color 与 rotate true，写入合法 inner XML，并在任何 package mutation 前严格拒绝 aliases、coercion、invalid ranges 与 unsafe object shapes。

Model/codec、SDK/root 与 adapter suites 分别为 234/234、197/197、79/79；跨 package focused gate 为 6/6。最终全量为 1280 passed / 1 skipped，独立 performance 为 1/1（607ms），两种 TypeScript build、两套 package build 与 declaration build 均通过。Actual 57-file tarball 的 installed Node/declarations/browser/CLI 均返回 `textShapeShadows: true`；真实浏览器 immediate/detached/reopen 完全匹配且 console/page/network errors 为 0，installed CLI PowerPoint 2010 validation 为 0 errors / 0 warnings。

### Text shape outer hyperlink

`AddTextOptions.hyperlink` 复用 strict mutually-exclusive `Hyperlink`，支持 non-empty XML-safe URL、当前 presentation 内一基 positive safe-integer slide target 与 optional direct tooltip。Plain/rich text、`addPlaceholder()`、placeholder population、slide/layout/master wrappers 和 declarative master text/placeholder objects 共用同一 normalizer/renderer；descriptor-safe ordinary/null-prototype 输入立即脱离 caller，unknown/accessor/symbol、missing/both target、coercible/dangling target 在任何 package mutation 前拒绝。

创建时同一 relationship ID 同时写入 non-visual shape `hlinkClick` 与每个非空 run 的 `hlinkClick`；空 paragraph/run 不新增引用。没有显式 underline 的 linked run 默认 `u="sng"`，`RichTextRunStyle.underline` 的 false/true/style 始终优先。Returned `ShapeModel.hyperlink` 只读取和编辑 whole-shape click：same-value exact no-op、whole replacement、clear、shared-ID clone-on-write 与 unreferenced GC 都不会覆盖 run links，因此既有 per-run state 可与之后的 shape click 分离。

URL/internal target/tooltip 可跨 duplicate、move、target delete、outer rollback、六格式 write/reopen 保存；duplicate self-link retarget duplicate，target deletion 清理 incoming shape/run click relationships。PptxGenJS 4.0.1 plain outer 合法输出具有同一 shape/run relationship 语义，但 omitted tooltip 总是物化 `tooltip=""`；rich outer 错误写多个悬空 `rIdundefined` 且没有 relationship。其合法 rich per-run 能力只写 run links 并为每个 run 分配独立 relationship；native 已对等该 ownership，并增加 outer inheritance、`false` suppression、strict read/edit、ID reuse、COW、GC 与 rollback。Native 接受 external run 的 `action=""`，但不复制宽松转换、orphan/dangling、falsy-underline 或 console-only 缺陷。

Model、SDK、root 与 PptxGenJS adapter suites 分别为 196/196、189/189、12/12、80/80；最终全量为 1290 passed / 1 skipped，独立 performance 为 1/1（584ms），TypeScript、两套 tsup 与 declaration build 全部通过。Actual 57-file tarball 的 installed Node/declarations/browser/CLI 与真实 Chrome 均返回 `textShapeHyperlinks: true`，Chrome validation/console/page/network errors 为 0。Internal-only installed CLI PowerPoint 2010 profile 为 0 errors / 0 warnings；external deck 仅有预期的 `OPC_EXTERNAL_RELATIONSHIP` portability warning，inspect/slides/part-read 均通过。

### Shape simple shadow

Strict `ShapeShadow`、`AddShapeOptions.shadow`、`AddTextOptions.shadow` 与 `ShapeModel.shadow` 支持唯一 direct `a:outerShdw` / `a:innerShdw` 的 create/read/whole-replace/clear。两个 kind 均支持 sRGB/theme color、量化到 `1/100000` 的 `0..1` opacity、量化到 1 EMU 的 `0..100pt` blur 与 `0..200pt` distance，以及量化到 `1/60000°` 的 `0 <= angle < 360`；outer 另支持 `rotateWithShape`。Omitted fields 使用 PptxGenJS 4.0.1 真实 shape output defaults：black、0.75、8pt、270°、4pt 和 outer rotate false；native 保留 explicit zero。输入在 mutation 前深度脱离，getter snapshot detached/deep-frozen；same-value exact no-op，same-kind edit patch owned spans，kind switch 只替换 shadow child，`undefined` 只移除 direct shadow 并保留 `effectLst` 与 glow/preset shadow/reflection/soft edge 等 sibling effects。Reader 对 malformed/ambiguous/wrong-namespace/effectDag/unsafe order 返回 `undefined`，setter 零 mutation 拒绝。PptxGenJS omitted 与 `type: 'none'` 映射 native `undefined`，legacy `offset` 对应 native `distance`；native 不复制其 zero falsy fallback、ignored rotate flag、hash/invalid/range passthrough 或 malformed inner closing tag。Generic effect stack、preset-shadow editing、custom shadow transforms，以及 image/table/chart/media 等其他 owner 的 shadow API 尚未支持。

### 门禁与实施顺序

每一行只有在 native `create/read/edit/delete/preserve/validate` 中适用的维度都有公开 API、单元测试、PptxGenJS public-output conformance、打包产物测试和真实 PPTX 兼容验证后才能标记完成。当前已依次完成 preset shape/adjustments、shape fill/simple line/arrows/click hyperlink/simple shadow、custom geometry paths、custom guide formulas、custom handles、custom connection sites、custom text rectangles、geometry evaluator、embedded raster bytes、raster source loader、image sizing/`srcRect`、SVG create/load/read/paired replace/sizing、strict embedded audio/video creation/public conformance、stable live media lifecycle、native media timing、九种标准 native chart 的创建与语义编辑、direct slide/layout/master background、direct slide/layout/master slide number、slide default text color、named master/layout/placeholder 创建编辑与六 domain population，以及 text shape simple fill/simple line/arrows/simple shadow/outer hyperlink/per-run rich-text hyperlink/preset geometry/rectRadius/isTextBox。

Rich-text `breakLine` 已完成；之后继续逐项补齐其余 advanced text，再进入 advanced table/`tableToSlides`、output/runtime helpers 与 peer-range full-suite audit。

### Named master/layout/placeholder

PptxGenJS 公开 API 把 definition 称为 master，但其 `masterName` 最终选择 slide layout。Native 保留该拼写，同时按标准关系链将 `defineSlideMaster()` 实现为真实 parent master 下的唯一 named layout。普通 rect/line/text/image/chart object 留在 layout 并由 slide 继承；placeholder prompt 也留在 layout，创建 slide 时只物化具有相同 name、type/index 和 geometry 的 empty owner。Name 或 `{ type, index }` selector 可分别填充 title/body 文字或 shape、pic image/SVG、chart、tbl table 和 media audio/video；错误 domain、歧义 owner 与重复填充在 mutation 前拒绝。

`SlideMasterModel` / `SlideLayoutModel` 是 stable live semantic wrappers，支持 direct background、slide number、shapes/placeholders 和可复用的内容创建方法。Whole-definition replace 保持 layout part URI、wrapper identity、master layout ID 和 incoming slide relationships；delete 对已使用 layout 要求 same-document replacement，先 retarget 后按 graph incoming 回收 owned dependencies。Background、content、placeholder relationship 与 payload 持久化；margin 为 PptxGenJS/OOXML 都不保存的 runtime hint，reopen 后为 `undefined`。

PptxGenJS 4.0.1 public-output conformance 覆盖 default/multiple named layouts、solid/transparency/PNG background、scalar/TRBL margin、slide number、rect/line/plain-rich text、PNG/SVG image、九种 chart 与 combo、六 placeholder types、empty owner 与六 domain population。比较以最终 relationship topology、object order/kind/transform/text/style、type/index、geometry、payload hash 与 chart/workbook agreement 为准，忽略 random GUID/rId、XML formatting 和 PptxGenJS fixed cache。Native 严格拒绝 unknown name、duplicate title/name/identity、invalid type、falsy-zero geometry 与 delayed write mutation，不复制 PptxGenJS 的 fixed slide-number ID、disabled master 或 noncanonical cache。

Focused suite 为 45 passed / 434 skipped，全量为 1256 passed / 1 skipped，performance 为 1/1（578ms），typecheck/build 通过。Actual tarball 为 57 files / 54 dist files；两次 clean build 的 sorted dist-hash manifest 与 tarball byte-identical，SHA-256 分别为 `0a8e958ccde379ae071a7388dc4c29278ac5033a8641976324fcd5820339ad27` 和 `8362a3af38a4a7e8316a7e49e8cb3f4fb405753bd20cc935db609441819ca5e8`。Packed Node/TypeScript/CLI/real-Chrome 均报告 `masterLayouts: true`；Chrome 返回两个 layout names、master solid/layout image background、六类 layout/slide placeholders、选中 relationship targets、三个 payload hashes、bar chart definition、reopen margin `null` 和 0 validation/console/page/network errors。

2 页 native gallery 为 32 parts / 29 relationships / 2 layouts / 1 master，PowerPoint 2010 profile 为 0 errors / 0 warnings；2 页 PptxGenJS control 为 36 parts / 34 relationships。八张 source/LibreOffice-round-trip 页面均为 2400×1350、180 DPI，全幅背景的 minimum non-white margin 按预期为 0px。Fixture 使用 1×1 黑色 PNG，native 第二页按测试意图重定向 blank default layout。LibreOffice 26.8 保留两页、两 layouts 和一 master，但改写 placeholder identity/slide-number cache，并移除 audio 与 embedded chart workbooks；不声明完整 round-trip。PowerPoint 16.112 对 native/control 均返回 `-9074` 且没有输出，不声明 PowerPoint 往返通过。

### Direct slide backgrounds

`SlideBackground` 公开 `SimpleFill | GradientFill | SlideBackgroundImage`。`SlideModel.background` 只投影唯一、namespace-correct 的 direct `p:sld/p:cSld/p:bg/p:bgPr`：`undefined` 删除 direct `p:bg` 并恢复继承，`{ kind: 'none' }` 写合法 `a:noFill`，solid 支持 sRGB/theme color 与量化到 0.001% 的 transparency，gradient 复用 strict linear/path state，image 支持 PNG/JPEG/GIF bytes。Getter 的对象、stops、rectangles 与 bytes 都与 package 脱离；相同 supported value 与 absent clear 是 parts/relationships/XML/graph/journal exact no-op。Malformed、ambiguous、wrong-namespace、`p:bgRef`、pattern/group fill、external/dangling/wrong-type image state strict read 为 `undefined` 且不修复原包。

Image replacement 同时管理 fill、relationship 与 media part。Duplicate 初始共享 target，首次不同写入 clone-on-write；shared relationship id 或与 picture 共用 target 时只隔离 background owner；replacement、clear 与 slide deletion 仅在 graph incoming 为零时收集 part。`PptxDocument.setSlideBackgroundImage()` 复用 raster source loader，接受 Node path、HTTP/HTTPS/browser-relative URL、strict data URI、bytes/ArrayBuffer、Blob/File、Web stream 与 async iterable，signature 和 optional MIME assertion 全部完成后才进入同步 transaction。Create/read/edit/clear/duplicate/delete/rollback/reopen 在 `pptx/pptm/potx/potm/ppsx/ppsm` 使用同一路径。

PptxGenJS 4.0.1 合法 solid、transparency 与 PNG background 的 direct choice、颜色/alpha、relationship target、content type 和 payload 均与 native supported state 对等。其 `{ type: 'none' }` 不写 direct background，等价于 native `undefined`；`{ type: 'none', color: 'FF3399' }` 会写空 `p:bgPr`，strict native reader 返回 unsupported `undefined`，而 native explicit none 始终写合法 `a:noFill`。这是已锁定的 intentional correction，不以 byte imitation 代替合法 OOXML。

Focused model/lifecycle、SDK six-format 和 PptxGenJS/validator suites 分别为 226/226、296/296、393/393。最终全量 Vitest 为 1156 passed、1 performance 默认 skipped；独立 performance 1/1、typecheck 与 build 通过。Packed Node、real-Chrome、declaration 与 installed CLI 均返回 `slideBackgrounds: true`；两次 clean build 的 48-file dist manifest 相同，SHA-256 为 `e42633dfd50e9f8731e780f6b911f691845c530f5c1a0b9e5f356f93a1a0f423`。

Native gallery 为 11 slides、41 parts、39 relationships、3 个 background media parts（PNG/JPEG/GIF 各一），PptxGenJS control 为 7 slides、47 parts、51 relationships；两者 PowerPoint 2010 profile 都是 0 errors / 0 warnings，18 页逐页 render 与 overflow 检查通过。LibreOffice 26.8 回存保留 11 页顺序、solid/gradient/image kinds 与全部 image payload hashes；它把 explicit noFill 改为 inherited、把 linear/path `rotateWithShape` 改为 false，并给 path gradient 加 full fill rectangle。回存件为 38 parts、36 relationships、0/0 validation。本机 PowerPoint 16.112 自动打开返回 `-9074`，本轮不声明 PowerPoint round-trip 通过。

### Direct slide numbers

Public `SlideNumberOptions` 覆盖 EMU x/y/width/height、left/center/right/justify、RTL、top/middle/bottom、point scalar/TRBL/partial margin，以及 font family/point size/language/bold/italic/sRGB/theme color/0–100 transparency；normalized `SlideNumber` getter 是 detached deep-frozen snapshot。`SlideModel.slideNumber`、`LayoutModel.slideNumber` 与 `MasterModel.slideNumber` 分别只拥有对应 part 的唯一 direct `p:ph type="sldNum"`，master 同时拥有 direct `p:hf@sldNum`；slide assignment 不暗写唯一 master/default layout。相同值、absent clear 和相同 start 是 exact no-op；unsafe namespace/duplicate/shape-id/field/run/paragraph/geometry/style state strict read 为 `undefined`，setter 在 mutation 前拒绝。

`CreatePresentationOptions.firstSlideNumber` / `PresentationModel.firstSlideNumber` 只接受 signed Int32 safe integer；`undefined` 恢复 OOXML 默认 1。Slide cache 是 start 加当前 zero-based index，layout/master cache 是 `‹#›`；start change 和 duplicate/move/delete 在同一 transaction 内同步安全识别的 direct slide fields。Diagnostics 精确区分 `SLIDE_NUMBER_SHAPE_ID_COLLISION`、`SLIDE_NUMBER_MASTER_DISABLED` 和 `SLIDE_NUMBER_CACHE_NONCANONICAL`。六种 presentation format 均使用同一路径，slide-number placeholder 不再被 title fallback 误识别。

PptxGenJS 4.0.1 default/full style、sRGB/scheme、三种 alignment 加 justify、三种 valign、scalar/TRBL margin、font/lang/bold/italic/transparency public output 已锁定。Native 不复制 fixed shape id 25 collision、zero-size truthy fallback、RTL/transparency 丢失、layout/master random cache 或 disabled master；public import 会诊断 legacy state，首次 explicit supported assignment 只 canonicalize 目标 owner。

Actual 54-file tarball 含 51 个 `dist` 文件，installed Node、real-Chrome、browser conditional export、declaration 与 CLI 均返回 `slideNumbers: true`。两次 package build 的 51-file manifest 完全一致，SHA-256 为 `3d77e6f56b8f299f2d580112fd0ebe77d0a98c38c07764259a3735064d5f9bea`。Focused suite 为 448/448；最终全量 Vitest 为 1194 passed、1 performance 默认 skipped，独立 performance 1/1、typecheck 与 build 通过。

Native gallery 为 16 slides、48 parts、45 relationships、PowerPoint 2010 profile 0/0；PptxGenJS control 为 16 slides、82 parts、95 relationships、0 errors 与 4 条预期 warning。32 页 180-DPI render、逐页检查和边界检查通过，最小 ink margin 为 50px/81px。LibreOffice 26.8 渲染全部 direct slide fields 但显示自己的 1..16 序列；回存保留标题顺序、15 个 direct owners、clear、field type、alignment 和主要显式样式，同时删除 `firstSlideNum` 并重写 cache/default font/language/layout/master placeholder，得到 0 errors 与 15 条 normalization warning。因此需要 LibreOffice 可见性的文稿应优先 materialize direct slide field，不只依赖 layout/master placeholder。本机 PowerPoint 16.112 启动但没有 active presentation 或输出文件，因此不声明 PowerPoint round-trip 通过。

### Slide default text color

`SlideModel.color` 公开 strict `Readonly<RichTextColor> | undefined` transient state。sRGB 规范化为无 `#` 的六位大写 hex，scheme 只接受已支持 DrawingML token；input descriptor-safe 且与 caller 脱离，getter snapshot frozen。Equal assignment 是 package/journal exact no-op，`undefined` 清除。Duplicate 复制当前 snapshot，move 保留，delete 清理，rollback 与 part-URI reuse 不会泄漏 transient state。

Default 只对设置之后新建的 `addText()` / `addRichText()` run 生效；显式 run `style.color` 优先，只有 transparency 而无 local color 的 run 继承 default。改变或清除 `slide.color` 不扫描、重染已有 shape/table，table、master、layout、placeholder 不继承。OOXML 没有合法 direct slide-level default-text-color 字段，因此 transient state 不序列化；颜色在 run 创建时物化为标准 `a:solidFill`，write/reopen 后 run 保持而 `slide.color === undefined`。

PptxGenJS 4.0.1 public-output conformance 覆盖 sRGB、scheme、plain/rich inherited、local override、transparency、temporal clear 与 table isolation；native 不复制非法 string delayed warning/fallback、lowercase getter 或硬编码 black zero-input default。`pptx/pptm/potx/potm/ppsx/ppsm`、Node、真实 Chrome、declaration 与 installed CLI 使用同一公开路径。

Focused 运行为 10 passed / 409 skipped；全量 Vitest 为 1205 passed / 1 skipped，独立 performance 1/1 为 998ms，typecheck 与 build 通过。Actual tarball 为 54 files / 51 dist files，dist manifest SHA-256 `467d87ffea6994355c357dbad3b1ea18afa8538b1bacb85b6de43de90ad16829`，tarball SHA-256 `6812000a83247fdf2d63eddf81ec6ffb43c721d478e4cdcbbf4c4a3ce2b65ad1`。Real-Chrome live/default/override/transparency/duplicate/reopen 状态完全匹配，console/page/network errors 为 0。

Native/PptxGenJS gallery 分别为 11/9 slides、38/52 parts、35/58 relationships，PowerPoint 2010 profile 均为 0 errors / 0 warnings。20 页 180-DPI render 全部逐页检查，overflow 为 0，minimum margin 均为 106px。LibreOffice 26.8 回存保留页序、文字顺序、custom sRGB/theme/override/40% transparency，只把 native `tx1` 规范化为等价 `dk1`；回存件仍为 0/0。本机 PowerPoint 16.112 对 native/control 均返回 `-9074`，未加载 presentation 或产生 PPTX/PDF，因此不声明 PowerPoint round-trip 通过。

### Embedded raster image creation, source loading, sizing, and source rectangles

`RasterImageContentType`、`AddImageOptions` 与 `SlideModel.addImage()` 已覆盖 strict `image/png | image/jpeg | image/gif` bytes 创建。输入必须是 non-empty `Uint8Array`，会立即复制；options 只接受 descriptor-safe ordinary/null-prototype own data properties。`contentType` 必填，name/alt text 与 native EMU/OOXML-angle transform 可选；defaults 为 x/y 0、width/height 1 inch、rotation 0、flip false、按页内现有图片数生成 `Image N`，以及 alt text `preencoded.png`。显式 empty name/alt text 保留 direct empty。每次调用原子拥有一个 unique media part、一个 internal image relationship 和 canonical rect/noChangeAspect/stretch picture；非法 bytes/MIME/XML/transform/unknown field 或中途失败均在 package/relationship/slide XML/mutation journal 上保持零变化。

Live `ImageModel` 在创建后立即与 `slide.shapes` identity 一致；duplicate 起初共享 media target，`replaceData()` 对 exclusive target 原位更新，对 shared target clone-on-write。Create/transform/replace/duplicate/rollback/write/reopen 与六种 presentation formats 均已覆盖。底层 `AddImageOptions.sourceRectangle` 与 live `ImageModel.sourceRectangle` 支持 direct `a:srcRect` create/read/whole-replace/repair/clear；四边是 1 = 1% 的 percent units，量化到 0.001%，允许 contain 所需负值，同时要求每边 `< 100`、横纵 pair sums 均保留正 source area。Getter detached/deep-frozen，同值赋值 exact no-op，`undefined` 清除 direct rectangle 而不改 payload、relationship、transform、effects 或 fill siblings；ambiguous duplicate ownership 零 mutation 拒绝。

高层 `PptxDocument.addImage()` 进一步接受 Node path、HTTP/HTTPS URL、browser-relative URL、strict canonical base64 data URI、`Uint8Array`、`ArrayBuffer`、`Blob`/`File`、Web `ReadableStream` 与 async byte iterable，并支持 AbortSignal。PNG/JPEG/GIF signature 是格式唯一事实；可选 `contentType` 只作 assertion，扩展名、Blob/HTTP MIME 与文件名均不参与检测。`inspectRasterImage()` 返回 canonical content type 与 raw pixel dimensions。`calculateRasterImageSizing()` 以 EMU frame 纯计算 contain/cover/source-pixel crop，结果及 `sourceRectangle` detached/deep-frozen；高层 `{ sizing }` 与 top-level width/height 互斥且拒绝 direct `sourceRectangle`。Sizing/options 在任何 async source I/O 前脱离 caller，计算在 package mutation 前完成，invalid options/source/MIME/extreme contain/out-of-bounds crop 都保持 package 零变化。

锁定 PptxGenJS 4.0.1 的公开 path/data 输出通过高层 loader 后与 native 最终语义匹配，另有 6 个 contain/cover/equal-ratio/crop public case 精确匹配最终 transform 与 direct `srcRect` integer percentages。Native 不复制其 zero/falsy fallback、console-only invalid rejection、path/data precedence、ambiguous outer/nested sizing dimensions 或 `image/jpg` MIME/extension quirk。Actual npm tarball 的 Node/browser/types/CLI smoke 均通过；连续两次 clean build 的 38 个 dist 文件 SHA-256 manifest 完全一致。

Source-loader actual-tarball gallery 为 4 页、32 shapes、12 images，覆盖 path、bytes、`ArrayBuffer`、data URI、`Blob`、Web stream、async iterable 与 HTTP URL。当前 sizing gallery 为 4 页、40 shapes、12 images，覆盖 landscape/portrait/square PNG/JPEG/GIF 的 contain/cover/equal ratio、full/center/edge/fractional crop、direct edit/clear、rotation/flips、special-character name 与 non-empty alt text。原件和 LibreOffice round-trip 均为 12/12 strict readable、PowerPoint 2010 validation 0 errors / 0 warnings、overflow 0，并已逐页视觉检查。

LibreOffice 保留 sizing gallery 的 12/12 payload SHA-256、content types、names、non-empty alt text、shape order 与 internal relationship modes；把 12 个重复 payload targets 去重为 3 个并重写 12/12 picture markup。最大 transform 量化 360 EMU，最大 `srcRect` 量化 0.007%，两项 flip 被等价规范化为 rotation +180°。原件 180 DPI 输出为 2400×1350；回存件规范化 slide width 后 direct 180-DPI raster 为 2401×1350，逐页检查使用 proportional 2400×1350 raster。这些是明确记录的 client normalization，不作为 native direct-state 对等。

### SVG image creation, source loading, fallback, sizing, and paired replacement

`SvgImageContentType`、`AddSvgImageOptions` 与 `SlideModel.addSvgImage(svgBytes, fallbackPngBytes, options?)` 已覆盖 canonical Office SVG picture 创建。底层两个 payload 必须分别是 non-empty detached `Uint8Array`，并由 low-level caller 保证声明与 bytes 一致；高层会先 strict inspect SVG 与 signature-valid PNG。SVG part 使用 `image/svg+xml`，fallback part 使用 `image/png`。Picture 的 direct blip 指向 PNG，固定 Office SVG extension URI/namespace 内的 `svgBlip` 指向 SVG；name、alt text、transform 与 direct `a:srcRect` 和 raster 共享 strict contract。两侧 part、两条 internal image relationship、XML 与 live identity 在一个 transaction 中创建，任何失败保持 package graph、ZIP state 与 mutation journal 零变化。

高层统一 `ImageSource` 已覆盖 path、HTTP/HTTPS、browser-relative URL、strict base64 data URI、bytes/ArrayBuffer、Blob/File、Web stream 与 async iterable 的 SVG。`inspectImage()` / `inspectSvgImage()` 解析 strict root namespace、UTF-8/XML 与 width/height/viewBox intrinsic dimensions；optional `contentType: 'image/svg+xml'` 只作 assertion。通用 `calculateImageSizing()` 对 SVG 支持 contain/cover/source-coordinate crop；省略 sizing 仍保持 x/y 0、width/height 1 inch。Fallback 优先级固定为 explicit PNG → browser Canvas → built-in transparent PNG；库不执行 SVG script、不抓取 external resource，也不承诺 arbitrary SVG rasterization fidelity。需要 fallback-only client 完整视觉的调用方必须提供高质量 PNG。

Namespace-aware reader 不依赖固定 prefix，并接受 relationship-identified LibreOffice `image/svg` part。安全 state 暴露 `ImageModel.isSvg`、`fallbackPartUri`、`svgPartUri`，且 `sourcePartUri === fallbackPartUri`。`replaceData()` 对任何 Office SVG extension candidate 明确拒绝；`replaceSvgData()` 在 exclusive canonical pair 上原位更新，在 duplicate/shared/noncanonical target 上对 SVG 与 PNG 两侧一起 clone-on-write，只 retarget 当前 picture。Malformed/ambiguous/external/dangling pair 不伪装成 editable SVG，paired replacement 在任何失败下完整回滚。

PptxGenJS 4.0.1 public conformance 为 3/3：data-contain、path-cover、data-crop 的 picture order、transform、direct `srcRect`、extension URI/namespace、relationship roles、SVG bytes、metadata 与 content types 均匹配。唯一刻意修复的缺陷是 PptxGenJS path SVG 会把 SVG bytes 放入 `.png` fallback part；native 每个 fallback 都通过真实 PNG signature 检查。PptxGenJS SVG import、arbitrary prefix 与 LibreOffice-normalized shared targets 均可 read/edit/reopen。

Actual npm tarball 的 Node/browser/declaration/CLI SVG smoke 通过；browser Blob/data URI 使用真实 Canvas PNG fallback，并验证每图两个 internal targets。连续两次 clean build 的 38 个 dist 文件 SHA-256 manifest 完全一致。5 页 gallery 包含 13 shapes、8 SVG pictures、7 SVG parts、7 PNG fallbacks 与 16 image relationships，覆盖全部 source forms、explicit/default fallback、contain/cover/crop、live `srcRect` edit、rotation/flips、duplicate/replacement、特殊名称和 alt text；原件与 LibreOffice round-trip 均 strict reopen，PowerPoint 2010 validation 0 errors / 0 warnings。

LibreOffice 保留 shape order、names、alt text、SVG payload hashes、relationship roles、extension URI/namespace 与 7 fallback + 7 SVG targets，没有新增 dedup；将 `image/svg+xml` 规范化为 `image/svg`，最大 position/size delta 360 EMU、最大 `srcRect` delta 0.003%，raw rotation 的 10,800,000 delta 来自 flip/rotation 等价规范化。回存后 LibreOffice 可能选择其 PNG preview/fallback 渲染，而不是 retained SVG，因此完整旧客户端视觉由 fallback 质量决定，不作为 SVG payload 丢失或 direct-state 不一致处理。

SVG create/load/read/replace/sizing 已从未支持项移出。仍未支持 external SVG relationship、SVG DOM 局部编辑、script execution、external-resource fetching、任意 SVG 栅格化一致性、image rounding/transparency、alt-text 编辑、hyperlink/shadow/placeholder，以及单图片删除与 media GC。Strict embedded media creation 已在下一节完成。

### Embedded audio/video creation and stable live lifecycle

`PptxDocument.addAudio()` / `addVideo()` 公开 strict embedded/external media 创建。`MediaSource` 接受 Node path、canonical base64 data URI、`Uint8Array`、`ArrayBuffer`、Blob/File、Web `ReadableStream` 和 async byte iterable；HTTP/HTTPS 媒体保留 external relationship 且永不抓取，其他 URL scheme 拒绝。Poster 接受相同 embedded source forms，但 HTTP/HTTPS poster 明确拒绝；省略 poster 时使用 built-in PNG。音频 MIME/extension 为 `audio/mpeg` `.mp3`、`audio/mp4` `.m4a`、`audio/wav` `.wav`、`audio/ogg` `.ogg`；视频为 `video/mp4` `.mp4|.m4v`、`video/quicktime` `.mov`、`video/webm` `.webm`；poster 为 `image/png` `.png`、`image/jpeg` `.jpg|.jpeg`、`image/gif` `.gif`。

Descriptor priority 固定为 explicit `contentType` assertion → data URI declaration → `fileName` 或 path/URL-path/`File.name` 的 known extension → domain default。Assertion 与 declaration 不一致、known extension 属于错误 domain 或与 resolved MIME 不一致均在 mutation 前拒绝；unknown extension 不作为事实，输出改用 resolved MIME 的 canonical extension。Strict data URI 只接受 `data:<supported-mime>;base64,<canonical-payload>`，要求 standard alphabet、完整 padding 与 canonical padding bits；empty、whitespace、URL-safe alphabet、percent encoding、extra comma 与 malformed padding 均拒绝。`Blob.type` 不参与 MIME 决策。Transcode output 只能是 ordinary `{ bytes: Uint8Array, contentType, extension? }`，其 optional extension 必须 lowercase 且精确匹配 output MIME。

Options 必须是 ordinary/null-prototype own-data object，拒绝 unknown/symbol/accessor/inherited 字段；memory bytes、ArrayBuffer、options 和 transcode input/result 会脱离 caller。所有 source/poster I/O、transcode、descriptor resolution、SHA-256 lookup 与 final XML definition 在 package mutation 前完成；随后 part/content-type/relationship/slide XML 写入一个 synchronous transaction。任何 create、source/poster replacement 或 deletion validation、I/O、transcode、hash、allocation、relationship、part 或 XML 失败都保持 package graph、ZIP state、对象 identity 与 mutation journal 零变化。

Embedded media 与 poster 只在 SHA-256 和 exact MIME 同时相等时复用 `/ppt/media` target。Canonical picture 写 `a:audioFile` / `a:videoFile`、standard kind relationship、embedded Microsoft media relationship、internal poster image relationship、media click action、rect/noChangeAspect/stretch poster、name/alt text 与 EMU transform。创建返回 live `MediaModel`；同一 session 中 `document.media()`、`slide.media`、`slide.shapes` 与创建结果为同一对象。`name`、`altText`、detached frozen `settings` 和 transform 可编辑；`replaceSource()` 保持 kind/identity 并支持 embedded↔external，`replacePoster()` 支持 PNG/JPEG/GIF，省略 source 重置 built-in PNG，`remove()` / `slide.deleteMedia()` 删除对象。

Duplicate 初始共享 media/poster targets；不同 replacement 采用 content-aware dedup 或 clone-on-write，只 retarget 当前 picture。共享 rId 时分配新 relationship，superseded relationship 仅在 XML reference count 为零后移除，target 仅在 package-graph incoming 为零后 GC。Move 保持 live handle，object/slide deletion 保留仍被引用的 targets。`play`、`loop`、`hideWhenStopped` 与 `volume` 同步写入 private exact-preference/ownership `px:playback` 与 native `p:timing`；`settings = undefined` 同时清除 extension 和库拥有的当前 media graph。

PptxGenJS 4.0.1 public conformance 为 4/4：audio data、video data、audio path 与重复 audio path 的 cover、`extn`、`objectName`、transform、payload、poster 与关系目标最终语义匹配。Import 兼容其 audio `a:videoFile`、`audio/mp3` 与 duplicate-audio Microsoft-media relationship 缺陷；read、metadata/settings/transform edit 不 canonicalize legacy primary roles，只有 `replaceSource()` canonicalize 当前 picture。Native 创建始终写 `a:audioFile`、canonical `audio/mpeg` 和 standard audio relationship。Native 同时严格拒绝 malformed base64、MIME/extension 冲突、非法 kind/geometry/options，且失败保持零 mutation，不复制 PptxGenJS runtime coercion。

Actual 45-file npm tarball 的 Node/real-Chrome/declaration/installed-CLI smoke 通过，`nativeMediaTiming: true`；连续两次 clean build 的 42 个 dist 文件 SHA-256 manifest 完全一致。六种 presentation formats 均覆盖 timing create/edit/duplicate/delete/reopen。9 页真实 gallery 含 12 个媒体对象、7 种媒体 MIME、3 种 poster MIME、10 个去重后的 `/ppt/media` parts 与零 orphan，覆盖 click/auto、loop、hideWhenStopped、volume 0/0.25/0.5/1、同页双媒体、普通动画共存、legacy materialization、native-only adoption，以及 duplicate/edit/clear/delete。原件 strict reopen，180-DPI render、overflow 与逐页视觉检查通过；PowerPoint 2010 profile 为 0 errors，只有 OGG/WebM 两条预期 warning。

LibreOffice 26.8 save/reopen 保留 9 页顺序与文案，但删除全部 media pictures、poster parts、media-role relationships 与 timing；Package 仍可 strict reopen，validator 为 0 errors / 0 warnings。该结果作为明确 client degradation 记录，不能解释为 native 媒体生命周期失败，也不能误报为 LibreOffice media round-trip 支持。本机 PowerPoint 16.112 自动打开对 gallery、LibreOffice 回存件与最小控制文件均返回 `-9074`，本轮不声明 PowerPoint round-trip 通过。

Data URI、cover/poster、MIME/extension mapping、object name、alt-text 创建、strict embedded audio/video creation、stable live identity/editing、完整 duplicate/move/delete isolation、media placeholder population、click/auto、loop、hide-when-stopped、volume native timing 与 public conformance 已从未支持项移出。仍待 online video、remote-fetch embedding、trim/bookmarks、有限重复、narration/cross-slide audio、captions/subtitles、crop/rounding/shadow/hyperlink/advanced placeholder styles、内建转码引擎与更广泛客户端认证；direct slide/layout/master background、slide number、default text color 与 named master/layout/placeholder 已完成，全局下一项为 advanced text。

### Native PowerPoint media timing

Media playback 现在由 core codec 直接创建，无需动画插件。`px:playback` 保存 strict 归一化偏好与 `mediaTnId`/`playTnId`/optional `pauseTnId` ownership，native `p:timing` 表达 PowerPoint 行为。Reader 优先返回合法 private preference；private state 缺失时，仅识别唯一、直接、完整、kind/target 匹配的 native graph。Native-only import 可读，首次相同值 assignment 只 adoption ownership 而不重写 timing；陈旧 ownership 会修复，unsupported/ambiguous graph 保持 bytes 并拒绝危险 edit。

Create、whole-settings edit、clear、duplicate、delete 与 materialization 都在一个 OPC transaction 内。Media 与 ordinary animation 共用全页 timing ID allocator；普通动画、未知 timing branches、peer media 与非目标 shape 不被误改。Legacy preference-only files 可通过 animation plugin 共享的 `MediaCodec.materializePlayback()` 幂等升级，健康或 unsafe imports 不会被重复物化。

六个 timing diagnostics 分别覆盖 missing、stale、unsupported、ambiguous、dangling target 与 kind mismatch。PptxGenJS 4.0.1 public `MediaProps` 本身没有 autoplay/loop/volume timing 输入，所以对等范围仍是其 public final media picture/relationships 的精确导入；native timing 是额外的 strict create/edit 能力，不虚构 PptxGenJS 不存在的选项。

Preset-shape geometry 通过 runtime-frozen `PRESET_SHAPE_TYPES` 和派生 `PresetShapeType` 暴露 178 个 canonical token。`SlideModel.addShape()` 默认 x/y/width/height 均为 1 inch，使用 direct `a:noFill` 与 empty `a:ln`；native transform 接受 EMU/OOXML angle，可通过 `inches()` / `degrees()` 转换。Options 只接受 ordinary 或 null-prototype object 的 own data properties，未知/inherited/symbol/accessor 字段和非法值在 mutation 前拒绝。新建 shape 与 live `ShapeModel` 保持 stable identity；same-value `presetType` assignment 是 exact no-op，type change 只 whole-replace direct geometry 并清除旧 adjustments，其他 transform/name/fill/line/arrows/effects/text/ext bytes 保留。Duplicate、rollback、六种格式、write/reopen、actual tarball Node/browser/types 以及锁定 PptxGenJS 4.0.1 的 public `ShapeType/addShape/write` 输出均有覆盖。PptxGenJS runtime 的 `folderCorner` 会写出不属于 OOXML `ST_ShapeType` 的无效 token；native 无损保留已有 bytes 但 strict preset snapshot 为 `undefined`，原生创建使用合法 `foldedCorner`。`custGeom` 不是 preset token，由 `addCustomShape()` / `customGeometry` 独立公开。

### Preset-shape adjustments

`ShapeAdjustment`、`AddShapeOptions.adjustments` 与 `ShapeModel.adjustments` 公开有序的 readonly `{ name, value }` direct guide 列表。Name 必须是唯一非空 XML-safe string；value 必须是 safe integer，并原样写为 `a:gd@fmla="val N"` 的 operand，不执行 shape-specific unit conversion、clamp 或 range guessing。输入必须是 dense ordinary array，entry 只能含 own data `name`/`value`，归一化 descriptor-safe、getter-free 且立即脱离 caller；getter 返回 detached deep-frozen snapshot。Setter whole-replace 整个 list，`[]` 保留 empty `a:avLst`，不接受 `undefined`；同一有序列表是 exact bytes/journal no-op。Reader 只接受唯一 namespace-correct direct `p:spPr/a:prstGeom/a:avLst`、direct same-namespace guides 和简单 `val` integer formula。Missing/repeated/wrong-namespace/duplicate/complex/malformed/unsafe state 读取为 `undefined`，编辑在 package 变化前抛 `ModelParseError`。不同 `presetType` 会重置 adjustments，同类型保留原 bytes；duplicate、rollback、六格式、write/reopen 与 stable identity 均已覆盖。

PptxGenJS 4.0.1 合法快捷字段最终输出可直接导入：`roundRect` 的 `rectRadius: 0.5`（w=4、h=2）得到 `adj=25000`；`angleRange: [270, 0]` 得到 `adj1=16200000`、`adj2=0`；fractional angles 按其 runtime 量化为 integer；`arcThicknessRatio: 0.5` 在有 angle range 的 block arc 上得到 `adj3=25000`。Native API 接受最终 guide list，因此保留 explicit zero、要求 number 且不实施 shortcut precedence。PptxGenJS 的 `rectRadius: 0` 与 zero thickness 会被 truthiness 丢弃，string 会转换，`rectRadius` 优先于 `angleRange`，没有 angles 时 thickness 被忽略，invalid input 还可写出 malformed/unsafe formula；adapter 无损保留这些 bytes，但 strict snapshot 对 unsupported state 返回 `undefined`。Custom geometry paths、guide formulas、handles 和 connection sites 使用独立 API。

Custom text rectangle 与 numeric geometry evaluation 均已支持；preset adjustment API 仍保持 direct guide-list 语义。

### Custom geometry paths

`CustomGeometryPoint`、`CustomGeometryCommand`、`CustomGeometryPathFill`、`CustomGeometryPath`、`CustomGeometry` 与 `AddCustomShapeOptions` 公开 strict custom path tree。`SlideModel.addCustomShape()` 从 blank presentation 直接创建 `a:custGeom`，`ShapeModel.customGeometry` 读取 detached deep-frozen snapshot 并 whole-replace 全部 geometry。Path extent 是 positive safe-integer direct EMU；numeric point/arc radius 是 direct EMU，numeric arc angle 是 direct `1/60000°`，也可使用 guide/built-in token；API 不推断 inch 或百分比，可显式调用 `inches()` / `degrees()`。六种 command 分支 `moveTo/lineTo/arcTo/quadraticBezierTo/cubicBezierTo/close`、多个 subpath、多个 path、empty command list、negative numeric point、zero numeric angle，以及 path `fill/stroke/extrusionOk` 均已覆盖。输入立即脱离 caller，unknown/missing/unsafe/非法顺序在 mutation 和 ID allocation 前拒绝；same-value 是 exact bytes/journal no-op，setter 不接受 `undefined` clear。

Custom ↔ preset 转换只替换 direct geometry，保留 live identity、name/transform、fill/line/arrows/shadow/hyperlink、text/effects/ext 与 relationship state。能力已覆盖 duplicate/source isolation、outer transaction rollback、六种 presentation formats、write/reopen、move/delete、unknown XML/opaque part preservation，以及 actual tarball Node/browser/types/CLI。Supported adjustment handles、connection sites 与 text rectangles 进入同一 strict snapshot 与 whole-replacement 生命周期；malformed direct state 保留原包 bytes，但 strict snapshot 返回 `undefined` 并在变更前拒绝 replacement。公开 evaluator 可在不修改 package 的前提下计算合法 snapshot。

### Custom geometry guide formulas

`CustomGeometryValue` 是 safe integer 或单个 non-empty/no-XML-whitespace/XML-safe/non-decimal token。`CustomGeometryGuide` 将 optional `adjustments` / `guides` 映射到 `a:avLst` / `a:gdLst`，name 在两个列表的合并命名空间中唯一。`CustomGeometryFormula` 以 exact readonly tuple 覆盖全部 17 个 DrawingML operators：一元 `val/abs/sqrt`，二元 `at2/cos/max/min/sin/tan`，三元 `*/`、`+-`、`+/`、`?:`、`cat2`、`mod`、`pin`、`sat2`。Point 和 arc fields 可引用这些 guide 或 built-in token。Normalization/getter 深度 detach/freeze guide、formula、operand 与 path tree；same-semantic assignment 是 exact bytes/journal no-op；malformed operator/arity/token/list/namespace 在 mutation 前拒绝。Strict codec 本身只保存/验证 direct tree；公开 evaluator 另行解析 dependency graph、cycle、forward reference、37 个 built-ins、arithmetic domain 与 numeric geometry。

PptxGenJS 4.0.1 没有 public guide-formula input，因此本能力是 native extension；原有合法 `custGeom + points` snapshot 保持无 empty optional properties。实际 npm tarball 的 Node/browser/types/CLI 已覆盖 formula create/read/edit/reopen 与 preset/custom conversion。4 页 formula gallery 含 9 个 formula shapes 和全部 17 个 operators；原始文件与 LibreOffice round-trip 文件均为 9/9 strict readable。LibreOffice 保留 adjustment/guide/formula 值和 path command 种类/顺序，并可将 `rect` 改写为引用 `textArea*` guides 的等价表达；该 normalization 现由 custom text-rectangle reader 严格支持。两份文件均通过 PowerPoint 2010 validation（0 errors / 0 warnings），无 overflow，逐页视觉复核一致。

### Custom geometry adjustment handles

`CustomGeometryXyHandle` 与 `CustomGeometryPolarHandle` 组成 ordered `CustomGeometryHandle` discriminated union，optional `CustomGeometry.handles` 保留 `a:ahLst` 中 XY/polar 的跨 kind 原始顺序。两类都要求映射 direct `a:pos` 的 `position`。XY 的 `xGuide/yGuide/minX/maxX/minY/maxY` 对应 `a:ahXY@{gdRefX,gdRefY,minX,maxX,minY,maxY}`；polar 的 `radiusGuide/angleGuide/minRadius/maxRadius/minAngle/maxAngle` 对应 `a:ahPolar@{gdRefR,gdRefAng,minR,maxR,minAng,maxAng}`。Position、XY/radius bounds 的 number 是 direct shape-coordinate safe integer，angle bounds 的 number 是 direct `1/60000°`，这些 value 也可为合法 token。所有 guide/bound fields 独立 optional：不要求 ref 与 bounds 同时出现、min/max 成对、guide 已解析或 numeric min <= max。Omitted/empty list 不产生 own `handles` property。

Handle input 与 snapshot 沿用 descriptor-safe normalization、caller detachment、recursive freeze 和 whole-geometry replacement；same semantic value 是 exact bytes/journal no-op。Strict reader 要求 namespace-correct `ahXY` / `ahPolar`、exact kind-specific attributes 与唯一 direct position；malformed handle 返回 `undefined` 且不修改 package。Actual tarball 的 Node/browser/types/CLI 已覆盖 create/read/edit/reorder/reopen、preset/custom conversion、duplicate、rollback 与六种 formats。PptxGenJS 4.0.1 public `custGeom + points` 始终只生成 empty `ahLst`，没有 arbitrary handle input，因此该能力是完整 DrawingML 创建/编辑所需的 native extension。

4 页 handle gallery 含 10 个命名 custom shapes，覆盖 XY x-only/y-only/two-axis、polar radius-only/angle-only/two-axis、mixed order、独立 omission、numeric/token bounds 与完整 lifecycle；原始文件与 LibreOffice 26.2 round-trip 文件均为 10/10 strict readable。Round-trip 保留 4 页、每页 8/10/13/15 个形状、shape order 与全部名称，同时可将 adjustment/guide/handle metadata 求值成 direct numeric paths，并写入四个 `textArea*` guides、custom rect 与 empty `ahLst`；这些表达均可严格导入。两份文件都通过 PowerPoint 2010 validation（0 errors / 0 warnings），无 overflow，并已逐页视觉检查。Formula 与 handle token/bound evaluation 已完成；handle dragging 与 resolved geometry bounds 仍未实现。

### Custom geometry connection sites

`CustomGeometryConnectionSite` 通过 optional ordered `CustomGeometry.connectionSites` 映射 direct `a:cxnLst`；每项映射一个 `a:cxn`，required `angle` 对应 `ang`，required `position` 对应唯一 direct `a:pos`。Position 的 number 是 custom-geometry coordinate-space direct safe integer，angle 的 number 是 direct `1/60000°`；两者也可使用 guide/built-in token，codec 只保留 token 而不求值。Source order 与 duplicate sites 原样保留；omitted/empty list 不产生 own `connectionSites` property。输入使用 descriptor-safe normalization 并立即 detach，getter 对 list/site/position 递归 deep freeze；赋相同 semantic snapshot 是 exact bytes/journal no-op，变更通过 `ShapeModel.customGeometry` whole-replace 完成。

Strict reader 要求 namespace-correct direct `cxnLst/cxn/pos`、唯一 required `ang/x/y`、合法 child order 和无额外状态；malformed site/list/namespace/attribute/value 返回 `undefined` 且零 mutation。Create/read/edit/reorder、preset/custom conversion、duplicate/source isolation、rollback、move/delete、六种 formats、write/reopen，以及 actual tarball Node/browser/types/CLI 均已覆盖。PptxGenJS 4.0.1 public `ShapeType.custGeom/addShape(points)/write` 只生成 empty `a:cxnLst`，没有 arbitrary connection-site input，因此本能力是完整 DrawingML 创建/编辑所需的 native extension。

4 页 connection-site gallery 含 6 个目标 custom shapes，覆盖 numeric cardinal sites、guide/built-in token sites、ordered duplicate sites 与 whole-replacement reorder；原件与 LibreOffice 26.2 round-trip 文件均为 6/6 strict readable。Round-trip 保留 4 页、每页 4/5/12/12 个形状、全部名称、shape order 与视觉外观，同时插入 custom text rectangle 及 `textArea*` / `GluePoint*` guides，并把角度归零、合并或丢弃 token/duplicate sites。这些客户端 normalization 会改变 connection-site 语义，但回存的 custom geometry direct state 仍可由 strict reader 完整读取。原件与回存件都通过 PowerPoint 2010 validation（0 errors / 0 warnings），无 overflow，并已逐页视觉检查。Connection-site numeric evaluation 已完成；connector snapping/creation 仍未实现。

### Custom geometry text rectangles

`CustomGeometryTextRectangle` 由 optional `CustomGeometry.textRectangle` 公开；required `left/top/right/bottom` 分别映射唯一 direct `a:rect@l/t/r/b`。四个边均使用 `CustomGeometryValue`：number 必须是 finite safe integer，string 必须是 non-empty/no-XML-whitespace/XML-safe/non-decimal guide 或 DrawingML built-in token。Omitted 与显式 `{ left: 'l', top: 't', right: 'r', bottom: 'b' }` 都折叠为 canonical default，不产生 own `textRectangle` snapshot property。

Normalization 是 descriptor-safe/getter-free，立即脱离 caller；getter 返回 detached deep-frozen rectangle；setter whole-replace 整个 custom geometry，same semantic value 是 exact bytes/journal no-op。Strict reader 将 absent rect 视为 canonical default；若 rect 存在，则要求它是唯一 namespace-correct direct `a:rect`，且只含 exact `l/t/r/b` 属性和合法四边。Repeated/wrong-namespace/extra-attribute/invalid-value 状态保留原包 bytes，但返回 `undefined` 并在变更前拒绝 replacement。Create/read/edit/reset-to-default、preset/custom conversion、duplicate/source isolation、rollback、move/delete、六种 formats、write/reopen，以及 actual tarball Node/browser/types/CLI 均已覆盖。

PptxGenJS 4.0.1 没有 arbitrary text-rectangle input，公开 `ShapeType.custGeom/addShape(points)/write` 只产生 canonical default rect；因此任意 rect 创建/编辑是完整 DrawingML 所需的 native extension。新增 4 页 gallery 含 10 个 text-bearing preset shapes 的原位 custom-geometry conversion，转换前后 `p:txBody` 完全保持；actual tarball strict reopen 为 10/10，PowerPoint 2010 validation 为 0 errors / 0 warnings，180 DPI render、overflow 与逐页视觉复核均通过。LibreOffice round-trip 保持页数、shape 数量/名称/顺序、文本与 path command kinds，会插入 `textAreaLeft/Top/Right/Bottom` guides 并把 rect 改写为这些 token；该文件仍为 10/10 strict readable，0 errors / 0 warnings。历史 numeric-path/formula/handle/connection-site galleries 的 LibreOffice 回存件现合计 39/39 non-default rect strict readable（14/14、9/9、10/10、6/6）。

PptxGenJS 4.0.1 public `ShapeType.custGeom/addShape(points)/write` 的合法最终输出可导入为相同 snapshot：首个 ordinary point 为 move，后续 ordinary point 为 line，`moveTo: true` 开新 subpath，arc/quadratic/cubic/close 映射直接 command；arc point `x/y` 不写入 OOXML。其 `<100` number 与 numeric string 按 inch、`>=100` number 按 direct value、percentage 按 full slide 尺寸计算，invalid curve kind 会静默省略，missing cubic control 会落为 zero；first curve、zero/negative radius 与 unsafe coordinate 可能生成 malformed output。Adapter 对合法 final paths 语义导入，对 malformed shapes 保持 bytes 且返回 `customGeometry === undefined`；native 只接受 explicit direct safe integers，不复制这些 heuristics/coercion/passthrough。

LibreOffice headless round-trip 会为 numeric-path gallery 的 custom geometry 注入 `textArea*` guide formulas、custom text rectangle 与空 shape text body；原始 `a:pathLst` commands 仍完整保留，PowerPoint 2010 validator 仍为 0 errors / 0 warnings。Artifact Tool 对原始及 round-trip gallery 均识别 5 页、43 个对象和全部 15 个命名 custom shapes，逐页对象数不变，shape bbox 最大差异为 0.04pt。Strict reader 现已支持 guide formulas 与 custom text rectangles；LibreOffice 回存后的 14/14 numeric-path `customGeometry` snapshots 可严格读取。这是可保留的客户端 normalization，不代表 path command 丢失。

### Custom geometry evaluator

`evaluateCustomGeometry(geometry, { width, height })` 是 pure public API；`ShapeModel.evaluateCustomGeometry()` 使用当前 live transform，在 preset 或 strict custom snapshot 不可读时返回 `undefined`。公开类型覆盖 `CustomGeometryEvaluationContext`、`CustomGeometryEvaluationErrorCode`、`CustomGeometryEvaluationError`、`EvaluatedCustomGeometryGuide/Point/TextRectangle/Command/XyHandle/PolarHandle/Handle/ConnectionSite/Path` 与 `EvaluatedCustomGeometry`。Standalone geometry/context 会先严格归一化并与 caller 脱离，shape 入口仅读取当前状态；两者都不修改 package，返回的 context、lists、entries、commands 与 points 全部 recursive frozen。

Evaluator 支持全部 37 个 built-in tokens：`3cd4/3cd8/5cd8/7cd8/b/cd2/cd4/cd8/h/hc/hd2/hd3/hd4/hd5/hd6/hd8/l/ls/r/ss/ssd2/ssd4/ssd6/ssd8/ssd16/ssd32/t/vc/w/wd2/wd3/wd4/wd5/wd6/wd8/wd10/wd32`，并执行全部 17 个公式 operator。它先按 source order 求 adjustment guides，再按 source order 求 shape guides；已求值的 custom guide 对后续表达式 shadow 同名 built-in，但定义该 guide 时同名 operand 仍读取 built-in。合法 finite fractional output 原样保留，`-0` 规范为 `0`，`*/` 与 `+/` 的 division-by-zero 返回 `0`。`unknown-token/forward-reference/cyclic-reference/invalid-domain/non-finite-result` 通过 typed error code 区分，并按适用情况保留 `guideName` / `token`。

Guide values、handle positions/bounds、connection-site positions/angles、text-rectangle edges 与所有 path command token 都会变成 number；省略的 text rectangle 会实体化为 `{ left: 0, top: 0, right: width, bottom: height }`。Path width/height/flags 与 handle guide-reference names 保持 direct state。当前边界是：不把 path coordinate system 缩放到 shape transform，不推导 arc endpoint，不计算 resolved geometry bounds，不实现 handle dragging，也不实现 connector snapping/creation。

PptxGenJS 4.0.1 无 public guide-formula、arbitrary handle/site/text-rectangle input，所以对等声明只覆盖其合法 `ShapeType.custGeom` 最终 numeric path 与 canonical default rect；其余 evaluation surface 是 native extension。Actual npm tarball 生成的 evaluator gallery 为 4 页、54 shapes、22 个 targets，原件 strict reopen 为 22/22。原件与 LibreOffice round-trip 均为 22/22 strict evaluable，PowerPoint 2010 validation 均为 0 errors / 0 warnings，180 DPI 输出均为 2400×1350、overflow 为 0，并已逐页视觉检查。

LibreOffice 对 22/22 targets 都改写 direct expression tree。Numeric path commands 与 effective text rectangle 在 21/22 上匹配；唯一差异是 `Operator sqrt` 的终点从 `600000` 改为 `0`。Guide arrays 为 0/22 原样匹配，因为 LibreOffice 全部改写为 numeric/text-area guides；combined handle/site target 的 handles 被移除、sites 被改写，因此 handles 与 connection sites 各为 21/22 direct match。这些差异被明确记录为 LibreOffice 保存行为；没有用视觉相似替代 direct-state 对等结论。

Shape simple fill 通过 `AddShapeOptions.fill` 与 `ShapeModel.fill` 支持 strict direct none 或 solid sRGB/theme color，可选 `0..100` transparency 并量化到 `0.001%`。Getter 返回 detached direct-state snapshot，不计算 inherited/effective color；same-value assignment 是 exact no-op，whole replacement 只替换 direct fill choice，`undefined` 清除 direct fill，`{ kind: 'none' }` 写入 direct `a:noFill`。Existing gradient/picture/pattern/group fill 在无关编辑中无损保留，可被 explicit simple fill 替换或清除，但 advanced fill creation 尚未进入该 API。PptxGenJS 4.0.1 explicit none 不写 direct fill choice，explicit zero transparency 不写 alpha；native 分别写 direct no-fill 与 `alpha val="100000"`，effective rendering 对等但 bytes/direct intent 不同。PptxGenJS empty fill 或 solid missing color 会落成 black，deprecated `alpha` 也会进入最终输出；native 在 mutation 前拒绝这些输入并只接受 `transparency`。

Shape simple line 通过 `AddShapeOptions.line` 与 `ShapeModel.line` 支持 strict direct none 或 solid line。Solid line 使用 sRGB/theme color、量化到 `0.001%` 的 `0..100` transparency、量化到 1 EMU 的 `0..1584` point width，以及 `solid/dash/dashDot/lgDash/lgDashDot/lgDashDotDot/sysDash/sysDot` 八种 preset dash；omitted width/dash materialize 为 1pt/solid，zero width 保留为 direct `w="0"`。Getter 只返回唯一、合法 direct `a:ln` 的 detached snapshot；empty/unsupported/malformed/ambiguous state 返回 `undefined` 且不修改 bytes。Same-value assignment 是 exact no-op；none 移除 owned width/dash 并写 line no-fill，`undefined` 只清除 owned width/fill/dash，保留 `a:ln` 容器、arrowheads、round/bevel/miter join、extensions、canonical unrelated attributes 和 relative order。Unique gradient/picture/pattern/group line fill 或 custom dash 可被 explicit simple line 替换或清除，但 advanced creation 不在该 API。PptxGenJS 4.0.1 omitted 与 explicit none 都输出 empty line，empty/missing-color 回退 `333333`，width zero 回退 1pt，zero transparency 省略 alpha，deprecated `alpha`/`lineDash` 被忽略；native 分别保留 explicit no-line、拒绝 missing color、保留 zero width 与 explicit-zero transparency 的 direct alpha，并使用 `dash` 映射其 `dashType`。Text-shape simple line、arrows、simple shadow、outer hyperlink、per-run rich-text hyperlink 与 preset geometry 已完成。

Arrow size、cap/compound/alignment/join 编辑、advanced line fill/custom dash 创建、generic/advanced effects、custom shadow transforms、non-shape/text shadows、custom geometry path scaling/arc endpoint and bounds/handle dragging/connector snapping and creation，以及 percentage positions 仍未支持；direct `isTextBox` 与 rich-text `breakLine` 已完成。

Shape arrows 通过独立的 `AddShapeOptions.arrows` 与 `ShapeModel.arrows` 支持 `none/arrow/diamond/oval/stealth/triangle` 六种 begin/end direct type。`ShapeArrows` 是 detached whole-replacement snapshot：missing side 清除该 endpoint，explicit `none` 保留 direct endpoint，`undefined` 或 empty snapshot 清除两端；same-value 是 exact bytes/journal no-op。Arrow editor 只拥有 direct `headEnd` / `tailEnd`，因此 arrow clear 保留 line width/fill/dash，反向的 line clear 也保留 endpoints；existing advanced line fill/dash、join、extension 和合法 `w` / `len="sm|med|lg"` size 在 type edit 中无损保留，但 size 不进入 snapshot，也未开放创建/编辑。PptxGenJS 4.0.1 对带 color 的六种合法 token 产生相同 final direct arrow state；arrow-only input 会额外物化 `333333`/1pt/solid，native arrows-only 则只写 endpoints，需要调用方显式组合 default `ShapeLine` 才对等该 final state。PptxGenJS `{ type: 'none', beginArrowType: 'triangle' }` 实际不写 noFill，和 native 只传 `arrows` 的 final state 对等；native 显式 `{ kind: 'none' }` 会保留 direct noFill intent。PptxGenJS runtime 忽略 empty string 与 nested `lineHead/lineTail`，映射 top-level deprecated aliases，并可透传非法 token；native 不接受这些 aliases，非法 value 在 mutation 前零变更拒绝，malformed existing endpoint 也不会被猜测或部分改写。

Text-shape arrows creation 通过 `AddTextOptions.arrows` 复用同一 `ShapeArrows` codec；和 preset shape 不同，text 的 omitted/arrow-only native line 保留既有 canonical direct `a:noFill`。Plain/rich、placeholder、layout/master 与 declarative owners 均已覆盖，line→head→tail 顺序、独立 ownership、explicit none、detachment、duplicate/rollback/reopen 和 PptxGenJS text-specific alias/invalid divergences 已锁定。Text-shape simple shadow、outer hyperlink、per-run rich-text hyperlink 与 preset geometry 也已完成。

`AddTextOptions.shape?: PresetShapeType` 已支持全部 178 个 canonical OOXML preset tokens，omitted/runtime `undefined` 默认 `rect`。Plain/rich、placeholder create/populate、slide/layout/master wrappers 与 declarative definitions 共用同一严格 normalizer/renderer；创建结果立即通过 live `ShapeModel.presetType` 读取和编辑。Geometry 与 fill/line/arrows/shadow/hyperlink/text/transform/placeholder identity 独立，duplicate、move、rollback、六格式 write/reopen 与 owner-source isolation 均已覆盖。

PptxGenJS 4.0.1 与 native 共有 177 个合法 token；其 runtime `ShapeType` 还暴露无效 OOXML `folderCorner` 和 custom-geometry `custGeom`，却缺少正确 `foldedCorner`。PptxGenJS 还会把 falsy input 当 omitted、把 unknown string/number 直接写入 `prst`，且 `shape: 'line'` 在缺少 line option 时抛错。Native 支持正确 `foldedCorner`，严格拒绝 falsy/unknown/coercion/`folderCorner`/`custGeom`，并把 `shape: 'line'` geometry 与 `AddTextOptions.line` style 分开处理，不复制上述缺陷。

最终全量为 1313 passed / 1 skipped，performance 为 1/1（578ms）；57-file actual tarball 的 Node/types/browser/CLI 与真实 Chrome 均报告 `textShapePresetGeometry: true`，Chrome validation/console/page/network errors 均为 0。代表性 2 页原件 PowerPoint 2010 validation 为 0/0；LibreOffice 回存保留全部 17 个 `(text, presetType)`，为 0 errors 与 2 条 placeholder-owner warnings。两份文件均无 overflow 且逐页视觉一致。

`AddTextOptions.rectRadius?: Emu` 已支持 `shape: 'roundRect'` 创建快捷语义。Native 接受 finite `0..914400` EMU、按最近 EMU 取整，并用最终 owner extent 计算 `round(rectRadius * 100000 / min(width, height))`；omitted/runtime `undefined` 保留 empty adjustment list，explicit zero 保留 `adj=0`。Plain/rich、placeholder create/populate、slide/layout/master wrappers 与 declarative definitions 共用同一实现。创建后的读取、替换和清空由 `ShapeModel.adjustments` 负责，resize 不自动重算 guide，fill/line/arrows/shadow/hyperlink/text/transform/placeholder identity 均保持独立。

PptxGenJS 4.0.1 合法正值在 2×1/0.5、4×2/0.5、4×2/1 与 fractional case 上产生相同 final guide。其 runtime 会丢失 zero 与 NaN、coerce numeric string、向非-roundRect 写 guide、透传负数/超范围，并可生成 `val Infinity`；native 对这些情况在 mutation 前严格拒绝，只保留可逆合法状态。

最终全量为 1320 passed / 1 skipped；model、SDK、root 与 adapter suites 为 203/203、195/195、15/15、85/85。57-file actual tarball、installed Node/types/browser/CLI 与真实 Google Chrome 均报告 `textShapeRectRadius: true`，Chrome validation/console/page/network errors 为 0。三页原件 PowerPoint 2010 validation 为 0 errors / 0 warnings，单项编辑只改变 `slide1.xml`，无 overflow 且逐页视觉通过。LibreOffice 渲染/回存保留 0、12500、25000、35000、50000、75000、100000 等全部显式 guides，只把 omitted 默认物化为 `16667`；回存件的 4 条 placeholder-owner warnings 来自 LibreOffice 自动加入的默认 layout placeholders，不是 radius package error。`AddTextOptions.isTextBox` 与 rich-text `breakLine` 已在后续阶段完成。

### Text shape `isTextBox`

`AddTextOptions.isTextBox?: boolean` 只管理 direct `p:sp/p:nvSpPr/p:cNvSpPr@txBox`。Omitted、own-data `undefined` 与 `false` 不写 attribute，`true` 写 canonical `txBox="1"`；inherited property 按 absent，accessor 不执行，defined value 只接受 primitive boolean。`ShapeModel.isTextBox` 读取 absence 为 false，接受 true aliases `1/true/on` 与 false aliases `0/false/off`；malformed token、qualified lookalike、重复 attribute 或歧义 owner 返回 `undefined`。Setter boolean-only，canonical 同值 exact bytes/journal no-op，单一 alias/malformed state 可修复，ambiguous state 零 mutation 拒绝。

Plain/rich、`addPlaceholder()`、layout/master direct methods、declarative text/placeholder definitions、duplicate/rollback/reopen 与六格式均已覆盖。Layout placeholder materialization 复制 source direct state；population 先验证 call-site 值，但最终由 layout source 覆盖，且不修改 source part。`isTextBox` 与 preset/custom geometry、adjustments/`rectRadius`、transform、text、fill/line/arrows/shadow/hyperlink 和 placeholder identity 保持独立。

PptxGenJS 4.0.1 合法 boolean public input 与 native final semantics 对等；其 runtime 任意 truthy 值也会写 true，native 严格拒绝。最终全量为 1337 passed / 1 skipped，performance 为 1/1（704ms）。Actual 57-file tarball 的 SHA-256 为 `2c6afd9bdb1f4c076ff0d0eb8bc8e8711793ae46dc320b2978d08f5e3a44b41a`；installed Node/browser conditional export/declarations/CLI 与真实 Google Chrome 均报告 `textShapeIsTextBox: true`，Chrome validation/console/page/network errors 为 0。

两页 QA deck 的 PowerPoint 2010 profile 为 0 errors，fixture 外部 hyperlink 产生唯一 1 条预期 portability warning。单项切换仅改变 `/ppt/slides/slide1.xml`，目标 `<p:cNvSpPr/>` 只增加 ` txBox="1"`，其余 21 parts byte-identical；原件与回存渲染均为 0 overflow 并通过逐页视觉检查。LibreOffice 保存会从 native 与独立 PptxGenJS 4.0.1 文件中移除所有 true `txBox` state，因此 source OOXML 是本项兼容判据，不声明该客户端 round-trip 保留。

### Rich-text `breakLine`

`RichTextRun.breakLine?: boolean` 是 transient input-only 字段：non-final run 的 primitive `true` 在该 run 后拆段，middle/trailing/empty/consecutive flags 按规范保留段落与中间空段，final flag 不生成尾部空段。拆分后的每个 segment 复制原 input paragraph 的 align、RTL、margin、indent、bullet、level、spacing 与 tab stops。`softBreakBefore` 和 `RichTextRunStyle.hyperlink` 均跟随 run；关系矩阵在 canonical paragraph/run indexes 上分配。

能力覆盖 slide/layout/master、placeholder prompt/population、declarative master、live edit、duplicate/move/rollback/reopen 与六格式。Getter 只返回 explicit `RichTextParagraph[]`，不暴露或反推 marker。Outer `AddTextOptions.breakLine` 不存在，run text CR/LF 也不是快捷方式。PptxGenJS 4.0.1 legal boolean output 的段落/属性/hyperlink final semantics 对等；native 严格拒绝 truthy/falsy string、number、null、object、boxed boolean。PptxGenJS 会抑制 split 后 first-run soft break，native 保留可逆 `softBreakBefore` direct intent。

最终全量为 1350 passed / 1 skipped，performance 1/1；两种 TypeScript check、Node/browser bundle 与 declaration build 全部通过。57-file actual tarball SHA-256 为 `d06b84c0c3b8ff8e610c87c55b0fe9b67de6b41e59b5ec7fad62b206fdbe2699`；installed Node/types/browser/CLI 与真实 Chrome 均报告 `richTextBreakLine: true`，Chrome validation/console/page/network errors 为 0。四页 source deck 为 PowerPoint 2010 0/0；单项链接 edit 只改变 slide XML 与该 slide relationship，其他 24 parts byte-identical。LibreOffice 保留 visible paragraphs、empty lines、soft breaks 与 internal link，但会合并 runs、省略 empty tooltip、下推 master content、重命名 placeholder，并丢弃 master placeholder prompt，因此不声明 owner identity 完整往返。

其余 advanced text/table、`tableToSlides`、output/runtime helpers 与 peer-range full-suite audit 仍待完成，不声明完整 PptxGenJS parity。

Shape click hyperlink 通过 mutually exclusive `Hyperlink`、`AddShapeOptions.hyperlink`、`AddTextOptions.hyperlink` 与 `ShapeModel.hyperlink` 支持 external URL 或当前 presentation 内部 slide target，以及 optional direct tooltip。输入必须是 descriptor-safe ordinary/null-prototype object，恰好包含一个 non-empty XML-safe URL 或 assignment 时存在的一基 positive safe-integer slide；unknown/accessor/symbol、missing/both target、coercible/dangling target 均在 mutation 前拒绝。Getter 返回 detached frozen direct-state snapshot，区分 omitted tooltip 与 explicit `tooltip: ''`；setter whole-replace target/tooltip，`undefined` 清除 click，same-value exact no-op。URL/slide 切换对 unshared relationship 原位更新，对 shared ID clone-on-write；clear/replacement 只 GC 已无引用的 relationship。Internal relationship 按 slide part identity 保存，因此 target 前 insert/delete/reorder 只改变 getter ordinal；duplicate self-link retarget duplicate，target deletion 清理 incoming DrawingML click/hover，rollback、六格式、write/reopen 与 live identity 保持隔离。Text outer 创建提供 shape click 与默认 run link；`RichTextRunStyle.hyperlink?: Hyperlink | false` 以显式独立 relationship 覆盖、省略时继承、`false` 时抑制，且显式 underline 优先。`ShapeModel.hyperlink` 仍只管理 whole-shape click，run-local state 通过 `ShapeModel.richText` whole replacement 编辑，支持 same-value、ID reuse、COW、GC 与 rollback。Run reader 只接受唯一 direct namespace-correct click 与 exact relationship/action ownership，并接受 PptxGenJS external run 的 `action=""`。PptxGenJS 4.0.1 omitted tooltip 会物化 empty；rich outer 错误写 `rIdundefined`，合法 rich per-run 只写 run links 且每 run 分配独立 relationship；native 不复制 orphan/dangling/falsy-underline/console-only 缺陷。该能力覆盖 slide/layout/master/placeholder/declarative owners、duplicate/move/delete、六格式、tarball Node/types/CLI 和真实 Chrome。External relationship 的 validator portability warning 是预期结果，不是 package error。Hover editor、table/image/chart/media/group/graphic-frame hyperlink creation、action/custom-show/macro/sound navigation，以及 relative/file safety policy 尚未支持。

LibreOffice headless 的 hyperlink 另存存在客户端限制：当前会从 native 与 PptxGenJS 4.0.1 文件中移除所有 direct click `tooltip`，测试中的 `a:hlinkHover` 也会被移除。页面展示顺序与物理 slide part 文件名顺序一致时，URL 和内部 slide target 均可保留；两者不一致时，LibreOffice 可能按 part 文件名错误重写内部 target，并可能丢失 duplicate slide 上的 self-link。因此 round-trip 结果不能作为原始 hyperlink identity 的判据；原始 OOXML relationship/action 已通过独立结构验证，这些差异属于 LibreOffice 保存行为。

PptxGenJS 4.0.1 的公开 `title` 默认值是 `PptxGenJS Presentation`，custom string 与显式 empty string 都写入 direct `dc:title`；adapter 通过公开 `write()` 输出可精确读取三种状态并在写出重开后保持。Native explicit custom/empty 生成相同语义与 XML escaping，但 native omitted 保持 `undefined`，不会注入 PptxGenJS 品牌默认值。Native 还可按 package-root core-properties relationship 读取、无损编辑或清除任意合法 part URI/prefix 的 direct title，并在 relationship 缺失时安全创建 part；same-value 与 absent clear 是 exact no-op，其他 core children 保留，malformed 或 ambiguous ownership 在写入时拒绝。

PptxGenJS 4.0.1 的公开 `author` 默认值是 `PptxGenJS`；default、custom `Alice & <Bob>` 和 explicit empty 都同时写入 direct `dc:creator` 与 `cp:lastModifiedBy`，并可由 adapter 精确读取、写出和重开。Native omitted creation 保留 canonical `@jiayunxie/pptx` creator；explicit custom/empty 对等 creator 的字符串语义与 XML escaping，但 native author 只拥有 direct creator，绝不覆盖独立 lastModifiedBy。`document.author = undefined` 只清除 creator；合法 existing deck 可按 root relationship 和 namespace URI 读取、编辑或创建 metadata part，same-value/absent-clear exact no-op，title、subject、revision、timestamps、unknown children 与其他 parts 保留。

PptxGenJS 4.0.1 没有独立 public lastModifiedBy field；其 public `author` 的 default `PptxGenJS`、custom `Alice & <Bob>` 和 explicit empty 都把 `cp:lastModifiedBy` 镜像为与 creator 相同的 direct state，adapter 可精确读取、写出和重开。Native zero-input 与 create-time `undefined` 保留 canonical `@jiayunxie/pptx` lastModifiedBy；strict custom/empty 对等合法字符串语义与 XML escaping，但 `document.lastModifiedBy` 只拥有 direct lastModifiedBy，绝不覆盖独立 creator。Assignment `undefined` 只清除该 field，不查找系统用户、不刷新时间戳也不递增 revision。Native 按 root relationship 和 core-properties namespace 读取、编辑或创建合法 part，支持 alternate URI/prefix、same-value/absent-clear exact no-op，并保留 title、subject、creator、revision、timestamps、unknown children 和其他 parts；malformed 或 ambiguous ownership 在 mutation 前拒绝。

PptxGenJS 4.0.1 没有独立 public created field；每次 public `write()` 会把当前时钟格式化为 UTC 秒级 direct `dcterms:created` 和 `dcterms:modified` typed properties。Adapter 只导入最终 OOXML 状态，`document.createdAt` 会按 element、XSI attribute 和 QName 的 expanded namespace读取合法 `W3CDTF` 值，并在 native write/reopen 后保持。Native omitted 和 create-time `undefined` 不写 created，strict explicit string接受 `YYYY-MM-DDTHH:mm:ss[.fraction](Z|±HH:mm)`、有效 Gregorian 日期及最大 ±14:00 offset；编辑可 repair simple invalid text或 missing/wrong unique type，`undefined` 只清除 created。相同合法值与 absent clear 是 exact no-op，modified、creator、lastModifiedBy、revision、unknown content和其他 parts保持不变。Native `write()` 不读取时钟、不自动刷新 created，也不把 modified或文件时间当 fallback。

PptxGenJS 4.0.1 同样没有独立 public modified field；public `write()` 生成 UTC 秒级 direct typed `dcterms:modified`。Adapter 按 expanded element/XSI/QName names读取同一次公开输出的 exact lexical value，native write/reopen保持该 final state。`document.modifiedAt` 与 createdAt 共用 strict `YYYY-MM-DDTHH:mm:ss[.fraction](Z|±HH:mm)`、Gregorian calendar和最大 ±14:00 offset contract，但拥有独立 direct modified：omitted/create-time `undefined` 不写，合法 replacement可 repair simple invalid text或唯一 missing/wrong type，`undefined`只清除 modified，same-value/absent-clear是exact no-op。Created、creator、lastModifiedBy、revision、unknown content和其他parts保持不变。Native `write()`不读取时钟、不自动刷新 modified，也不从created、file time或其他metadata派生值；对等范围是PptxGenJS公开输出的最终typed状态，而不是其隐藏clock side effect。

PptxGenJS 4.0.1 的公开 `subject` 默认值是 `PptxGenJS Presentation`；default、custom `Revenue & <Forecast>` 和 explicit empty 都写入 direct `dc:subject`，并可由 adapter 精确读取、写出和重开。Native omitted creation 保持 canonical core-properties bytes，snapshot 为 `undefined`；explicit custom/empty 对等字符串语义与 XML escaping，`document.subject = undefined` 只清除 direct subject。Native 按 root relationship 和 namespace URI 读取、编辑或创建合法 metadata part，接受 alternate URI/prefix，same-value/absent-clear exact no-op，并保留 title、creator、lastModifiedBy、revision、timestamps、unknown children 和其他 parts。Malformed 或 ambiguous ownership 在 mutation 前拒绝。

PptxGenJS 4.0.1 的公开 `revision` 默认值是 `'1'`；`'0'`、`'42'` 和 `'007'` 原 lexical string 写入 direct `cp:revision`，并可由 adapter 精确读取、写出和重开。Native zero-input 与 create-time `undefined` 同样保留 canonical `'1'`；strict API只接受一个或多个 ASCII digits，保留 leading zeros，`document.revision = undefined` 只清除 direct revision且不刷新 lastModifiedBy 或 timestamps。Native按 root relationship 和 core-properties namespace读取、编辑或创建合法 part，same-value/absent-clear exact no-op，并保留 title、subject、creator、lastModifiedBy、timestamps、unknown children和其他 parts。PptxGenJS runtime不会执行其公开文档的 whole-number约束，可能写出 empty、negative、decimal或alphabetic值；adapter忠实保留这些 bytes，但 strict snapshot返回 `undefined`，native创建/编辑在 mutation前拒绝。

PptxGenJS 4.0.1 的公开 `company` 默认值是 `PptxGenJS`；default、XML-safe custom `Acme 国际` 和 explicit empty 都写入 extended-properties direct `Company`，并可由 adapter 精确读取、写出和重开。Native omitted creation 保持 canonical app-properties bytes，snapshot 为 `undefined`；explicit custom/empty 对等合法字符串状态，`document.company = undefined` 只清除 direct Company。Native 按 root extended-properties relationship 和 namespace URI 读取、编辑或创建合法 part，接受 default/prefixed namespace，same-value/absent-clear exact no-op，并保留 Application、AppVersion、PresentationFormat、统计、vectors、链接状态、unknown children 和其他 parts。PptxGenJS 4.0.1 将 company 原样插值到 XML，因此含 `&` 或 `<` 的值会产生 malformed app.xml；native 会正确 escape，而不是复制该 runtime 缺陷。

PptxGenJS 4.0.1 的公开 `theme` 默认和显式 `{}` 都生成 `Calibri Light` / `Calibri`，head-only、body-only 和 full custom 会分别对缺省字段应用相同 fallback；adapter 通过 public `write()` 导入最终的 presentation theme state。Native zero-input 有意保留 canonical `Aptos Display` / `Aptos`，只有显式 `{}` 或 partial `CreatePresentationOptions.theme` / `document.theme` 才采用 PptxGenJS fallback。输入只接受 descriptor-safe ordinary 或 null-prototype object、受支持的 own data properties 和非空 XML-safe string；不会复制 PptxGenJS 对空白、非 string 或未 escape 输入的 runtime 缺陷。`document.theme` 返回 detached full snapshot，assignment 是 whole replacement；具体 `ThemeModel.setFonts()` 是 partial direct editor。读取和编辑只沿 presentation part 唯一 internal direct theme relationship 定位，并只拥有 DrawingML `majorFont/latin@typeface` 与 `minorFont/latin@typeface`；不会按 part 顺序猜测，也不会改 panose、East Asian/complex-script/script fonts、theme colors、format scheme、extensions 或其他 parts。

PptxGenJS 4.0.1 通过 PowerPoint 2010 `p14` section extension 按 presentation slide ID 保存 section 顺序、名称与 membership；其随机 section GUID 在公开输出对照中只作为 opaque identity 处理，adapter 通过 public `write()` 导入最终 title/order/membership。Native `addSection()` 与 `addSlide({ sectionTitle })` 对等合法 final state，duplicate title 选择第一个 exact match；sections 已启用时，未指定 title 的新 slide 会创建或延续 canonical `Default-N`。Native 对 unknown title 在创建 slide 前抛错，并让 `order: 0` 真正插入首位，不复制 PptxGenJS 的 warning-only loose slide 与 falsy append 缺陷；也不会合成无关的 PowerPoint 2012 slide-guide extension。`document.sections` 对 absent 或合法 empty list 返回 detached `[]`，对歧义或不安全 state 返回 `undefined`；ID 寻址的 rename/move/delete/assign 命令支持 existing deck 无损编辑，删除 section 只让成员 slides 变为 loose。Slide duplicate/delete/move 会在同一 transaction 内保持 membership 合法并按新的全局 slide order 排序。Empty section、loose slide、unknown extension siblings 与未拥有 XML 均保留。Custom properties，以及 masters、layouts、placeholders 的完整公开 API 对等仍待后续小项补齐。

PptxGenJS 4.0.1 的公开 `slide.hidden` omitted 与 explicit false 都不写 slide root `show`，true 与 truthy 非 boolean runtime value 都写 `show="0"`；adapter 只通过 public `write()` 导入这四种最终状态。Native 对合法 boolean final state 对等但拒绝 coercion：`SlideModel.hidden` 将 direct root `show` absence、`1/true/on` 读为 visible `false`，将 `0/false/off` 读为 hidden `true`，未知 token 或不安全 ownership 读为 `undefined`。赋值 true canonicalize 为 `show="0"`，赋值 false 删除 direct unqualified attribute；qualified lookalike、descendant、未知 XML 和其他 parts 均保留。Duplicate 保持 hidden bytes，move 不改变 visibility，delete 使用既有 slide lifecycle，section membership 与 hidden 正交，六种 OOXML presentation format 均可 write/reopen。

PptxGenJS 4.0.1 的 public `addNotes(string)` 和 omitted call 都会为每张 slide eager 创建 notesSlide；两者的 empty body 经 adapter 均读取为 `''`。Native `SlideModel.notes` 有意保留 lazy absence：未创建为 `undefined`，显式 empty 为 `''`，赋值 `undefined` 只删除该 slide 的 notesSlide relationship 与 owned part。合法 string creation/edit 与 PptxGenJS final text state 对等，CRLF/CR 规范化为 LF，XML metacharacters 正确 escape；number、object 与非法 XML controls 在 mutation 前拒绝。读取和编辑只拥有 unique direct notes body placeholder 及其 direct text paragraphs，并验证 slide→notesSlide、notesSlide→slide、notesSlide→notesMaster 和 presentation→notesMaster 拓扑；缺失 body 可最小修复，fully absent master state 可按 presentation direct theme、否则 first ordered slide-master theme 创建 canonical master，partial/ambiguous state 不猜测。Duplicate 克隆并 retarget per-slide notes part、共享 master，move/delete/sections/hidden/rollback/六格式 reopen 保持隔离。Rich notes、notes-page layout、comments、header/footer/date/slide-number controls 不属于本 plain-text 能力。

已完成 direct slide/layout/master background、direct slide/layout/master slide number、transient default text color、named layout selection、semantic master/layout wrappers 与六类 placeholder population。剩余 slide-level 对等缺口主要是完整 theme text cascade、percentage coordinates、custom shows 与放映范围，以及 shape 高级样式、SVG advanced、其他 image 样式/生命周期和 advanced media/chart 能力。标准 native chart 与 embedded media 的创建、编辑和生命周期已移入支持项。

基础 table creation 与 PptxGenJS 4.0.1 的 plain table 输出在 table URI、geometry、grid/row 总尺寸、cell text、direct paragraph alignment、direct margins 和 direct no-fill borders 上对等。原生 cell 可为 string 或 `{ text: string, options?: { align?, border?, fill?, fit?, margin?, textDirection?, valign? } }` ordinary/null-prototype object，并在同一 matrix 混排；cell/options/margin/border/side/fill/color 必须只有受支持的 own data properties，归一化后立即与 caller 深度脱离。相同 text 的 string、`{ text }`、empty options、undefined align/border/fill/fit/margin/textDirection/valign、explicit fit none、empty margin、等价 empty border 和显式 `horz` 生成 byte-identical native OOXML；原生严格拒绝其他 cell options，不会静默忽略尚未实现的样式。`AddTableOptions.textDirection` 与 `AddTableCellOptions.textDirection` 接受 strict `horz`、`vert`、`vert270`、`wordArtVert`；table 值实体化到 omitted/`undefined` cell direction，任何 explicit cell 值优先，其中 cell `horz` 阻断非水平 table 值但仍不写 token；最终 resolved omitted/`undefined`/`horz` 不写 direct token，三个非水平值写 physical-cell `tcPr@vert`。`AddTableOptions.align` 将严格的 left/center/right/justify 默认值实体化到未提供 cell align 的 physical cells，合法 cell align 优先；`AddTableOptions.margin` 以 point scalar/TRBL/named sides 覆盖 canonical margin，cell margin 再逐边覆盖并实体化到 physical cells；`AddTableOptions.valign` 将严格的 top/middle/bottom 默认值实体化到未覆盖 cells；`AddTableOptions.border` 将 strict scalar/TRBL/named whole value 实体化到没有有效 cell border 的 cells，任意非空 cell border 完整阻断 table 值；`AddTableOptions.fill` 将 strict solid/none whole value 实体化到没有 cell fill 的 cells，cell solid/none 完整覆盖 table 值。六类 table default 在 omitted/`undefined` 时保持原 bytes，最终只保留 physical-cell direct state，不保留 table metadata；创建后的 cell clear/replace 因此不会重新继承。原生 API 返回 live `TableModel`，x/y 缺省为 0.5 inch、每列缺省 1 inch、总高缺省 1 inch，并修正 PptxGenJS 省略 width 时 xfrm width 为 0 而 grid 非零的不一致。`columnWidths` 以 EMU 接受 scalar 或严格等长数组：scalar 精确重复，数组按列保留，省略 width 时由列宽总和推导，显式 width 必须与总和一致。PptxGenJS 4.0.1 的 scalar `colW` 会先取整、单项数组被当作 scalar shortcut、数组长度不匹配会回退均分，且 array `colW` 可生成 xfrm/grid 总宽不一致；原生 API 对这些情况分别保留精度、要求精确列数并保持 transform/grid 一致。已有表格通过 `TableModel.columnWidths` 暴露 detached strict grid snapshot，并由 `setColumnWidths()` 原子编辑 direct `gridCol@w` 与 `ext@cx`；valid mismatch 会修复为 grid sum，malformed/ambiguous grid 读取为 `undefined`、写入则零 mutation 拒绝。PptxGenJS 没有已有 deck 对象模型，因此这部分是原生 lossless editing 扩展。`rowHeights` 同样以 EMU 接受 scalar 或严格等长数组；省略 height 时由行高总和推导，显式 height 必须相等，而完全省略时仍保留 `tr@h=0` 自动行高。已有表格通过 `TableModel.rowHeights` 暴露 direct `tr@h` detached snapshot，zero 保留为自动行高；`setRowHeights()` 对全正目标同步 `ext@cy` 并修复 valid mismatch，对含 zero 目标只更新 rows 并保留合法 transform height。PptxGenJS `rowH` 会把单项数组隐式广播、对 short/falsy item 混入 overall height 回退、忽略 long array 多余项，并在省略或冲突的 `h` 下生成 xfrm/rows 总高不一致；原生 API 严格拒绝这些模糊输入，且把 existing-deck 自动行高编辑作为 lossless extension。当前仍不支持 table-level fit creation、merge/colspan/rowspan 和 hyperlinks；table-level direction/border/margin/fill/valign getter/editor、diagonal/advanced borders、advanced fills、rich/multi-paragraph text 与 alignment、row insertion/deletion、table/cell creation styles、auto-page/repeated headers、内容测量与 layout recomputation 也尚未支持。

LibreOffice headless 可无修复打开 underline 文件，但当前会把 double/dash/wavy 和独立 underline color 降级显示为普通单实线；同一 PptxGenJS 4.0.1 对照文件表现一致。OOXML token 与颜色仍保持合法并可由支持这些样式的客户端读取。

LibreOffice headless 当前也不显示 run-level single/double strike；同一 PptxGenJS 4.0.1 对照文件表现一致。三个合法 `strike` token 仍保留在 OOXML 中。

PptxGenJS 4.0.1 的 `margin` tuple 注释声明 `[top, right, bottom, left]`，但 runtime 对非对称值会交换 top/left。本库采用公开文档顺序，并提供具名 `{ top, right, bottom, left }` 输入；adapter 仍会忠实读取 PptxGenJS 已生成的实际 OOXML。

文本框 `valign` 省略时原生 API 与 PptxGenJS 4.0.1 都显式使用 middle；top、middle、bottom 分别映射到 direct `bodyPr@anchor` 的 `t`、`ctr`、`b`。PptxGenJS 放在单个 rich-text run 上的 `valign` 不生效，本库也只在文本框级公开该选项。

文本框 `wrap` 省略或为 true 时原生 API 与 PptxGenJS 4.0.1 都写 `bodyPr@wrap="square"`，false 写 `none`。PptxGenJS 对非法值静默回退 true、并忽略单个 rich-text run 上的 `wrap`；本库只在文本框级接受严格 boolean。

文本框 `vert` 支持 `eaVert`、`horz`、`mongolianVert`、`vert`、`vert270`、`wordArtVert`、`wordArtVertRtl`，并原样映射到 direct `bodyPr@vert`。省略值不写属性，因此与显式 `horz` 不同；`shape.textDirection = undefined` 只清除 direct override。PptxGenJS 4.0.1 会把非法字符串原样写入，但本库创建和编辑严格拒绝，读取未知 token 返回 `undefined` 且保留原始 XML。普通文本框的 `textDirection` 别名以及 run 级 `vert` / `textDirection` 在 PptxGenJS 中不生效。

Table/table-cell `textDirection` 是独立的四值能力：`AddTableOptions.textDirection` 与 `AddTableCellOptions.textDirection` 都接受 `horz`、`vert`、`vert270`、`wordArtVert`，归一化后立即与 caller 脱离。创建时 table 值实体化到 cell value omitted 或 runtime-`undefined` 的 physical cells；任何 explicit cell 值优先，因此 cell `horz` 会阻断非水平 table default，但创建期仍省略 `tcPr@vert`。Resolved omitted/`undefined`/`horz` 都不写 direct attribute，三个非水平值写 exact physical-cell token；table omitted/`undefined`/`horz` 在无 cell override 的等价输入下生成相同 direction bytes。最终只保留 physical-cell direct state，不保留 table metadata，所以后续 clear 不会重新继承。Live snapshot、支持的无关 cell edit、duplicate、rollback、write 与 reopen 保持该 final state。`TableCell.textDirection` 只读取唯一 direct `tcPr` 上唯一、无 namespace 且精确合法的 token；`setCellTextDirection()` 以零基 physical row/cell index 编辑并保留 lossless direct-state 语义，因此显式 `horz` 写属性，`undefined` 只清除该属性。PptxGenJS 4.0.1 对 supported table/cell 创建、cell precedence 与 horizontal collapse 产生相同最终 direct state；runtime 放行的 `eaVert` 等类型外 token 严格读为 `undefined` 并原样保留，native 创建则在 mutation 前拒绝。Table-level direction getter/editor 仍未支持，并与 cell fit 独立。

Table/table-cell 水平对齐创建通过 `AddTableOptions.align` 与 `AddTableCellOptions.align` 复用严格的 `TextAlignment`：`left`、`center`、`right`、`justify` 分别映射到每个 cell 唯一单段落 direct `a:pPr@algn` token `l`、`ctr`、`r`、`just`，不在 `tcPr`、`bodyPr` 或 table metadata 写同名属性。table 值实体化到 omitted 或 runtime-`undefined` cell align，合法 cell 值优先；table align omitted 或 runtime-`undefined` 时保持原 bytes，也不合成 effective left。创建后仅保留 physical-cell direct state，不保留 default source，因此后续清除不会重新继承。`TableCell.horizontalAlignment` 只在唯一 direct `txBody` 中存在唯一 direct paragraph 和唯一 direct `pPr` 时读取无 namespace `algn`，精确映射四个 token；缺失、malformed、ambiguous、zero/multi-paragraph 状态返回 `undefined`，且不计算 effective default。`setCellHorizontalAlignment()` 以零基 physical row/cell index 写 canonical token，`undefined` 只清除 direct unqualified attribute；同值与 absent clear 是 exact no-op，unsafe structure 以零 mutation 拒绝，其他 paragraph/cell XML 保持不变。PptxGenJS 4.0.1 的合法 table/cell 值和 cell precedence 会物化为相同 direct state，并可由 adapter 精确读取和继续编辑；未知 table runtime 值仍会被 PptxGenJS 静默丢弃，而原生创建/编辑在 mutation 前抛 `TypeError`。已有 deck 编辑是 lossless native extension，因为 PptxGenJS 没有 existing-deck editor；rich/multi-paragraph cell alignment 仍未支持。

原生创建字段 `AddTableOptions.valign`、`AddTableCellOptions.valign` 和 snapshot 字段 `TableCell.verticalAlignment` 都复用严格的 `top`、`middle`、`bottom`，映射物理 cell 的 direct `tcPr@anchor="t/ctr/b"`，绝不读写 cell `bodyPr@anchor`。table 值会实体化到未提供 cell 值的位置，cell 值优先；两层都 omitted/`undefined` 时不写 anchor，并与等价 string cell 保持 byte-identical。`marL/marR/marT/marB` 后写 optional `anchor` attribute，再写 L/R/T/B borders 与 fill。live `TableModel` 会立即暴露解析后的每个 cell 值；创建后不保留 default source，所以 `setCellVerticalAlignment(..., undefined)` 清除 direct anchor 后不会重新继承。getter 要求唯一 direct `tcPr`、唯一无 namespace anchor 和精确 wire token，不合成 effective default；editor 不支持 `just/dist`。PptxGenJS 4.0.1 对合法 table/cell 值生成相同最终 direct state、实体化与覆盖顺序。PptxGenJS runtime 会透传类型外 token，adapter 严格导入为 `undefined`，而原生创建/编辑会直接拒绝。table-level getter/editor 仍未支持。

Table-cell `margins` 复用 point-based `TextBoxMargins` value shape，但只管理唯一 direct `tcPr` 上的 `marL/marR/marT/marB` signed-Int32 integer，不读取 `bodyPr@*Ins`、style 或 effective default。`AddTableOptions.margin` 与 `AddTableCellOptions.margin` 都接受 point scalar、精确 TRBL tuple、partial named object、`{}` 或 `undefined`；descriptor-safe、getter-free、ordinary/null-prototype-only 的归一化会立即脱离 caller。创建按 canonical top/bottom 3.6pt、left/right 7.2pt → table sides → cell sides 逐边解析；cell scalar/TRBL 覆盖四边，partial named 只覆盖提供边，omitted/undefined/empty cell margin 继承 table，omitted/undefined/empty table margin 维持原 bytes。最终只实体化 physical-cell direct attributes，不保留 table metadata；`TableCell.margins` 立即显示解析值，之后清除 cell 不会重新继承。zero、negative、positive、fractional point 均量化到最近 EMU，但 raw 必须落在 signed Int32。创建按 `marL/marR/marT/marB` attributes、optional anchor attribute、L/R/T/B border children、fill 的固定顺序输出。`setCellMargins()` 使用零基 physical row/cell index，接受相同 value shape 但执行 whole replacement，未提供的边会清除；合法同数值为 exact no-op，malformed direct token 可由合法值覆盖，或因该边缺省而清除。PptxGenJS 4.0.1 省略 margin 时写相同 narrow defaults，并以相同 table→cell 层级实体化最终 direct state；其遗留 runtime 以第一项 `<1` 按 inches、`>=1` 按 points。因此 native table/cell `7.2` 对等 PptxGenJS `0.1`，native `[3.6, 7.2, 10.8, 14.4]` 对等 PptxGenJS `[0.05, 0.1, 0.15, 0.2]`；native `0.1` 则有意表示 0.1pt 而不是 0.1in。adapter 忠实读取最终 EMU 并统一暴露 point，不逆推输入单位。table-level margin getter/editor 与 layout recomputation 仍未支持。

Table-cell `borders` 只管理选中物理 cell 唯一 direct `tcPr` 下同 prefix 的 `lnL/lnR/lnT/lnB`，以 partial `{ top, right, bottom, left }` snapshot 暴露 strict direct state。side value 是 `{ kind: 'none' }` 或 `{ kind: 'line', color, width, style? }`；width 为 `0..1584` point 并量化到 EMU，color 支持 strict sRGB/theme，style omitted/solid/dash 分别写 no direct dash、`solid`、`sysDash`。`AddTableOptions.border`、cell `options.border` 与 `setCellBorders()` 都接受 scalar、精确 TRBL tuple 或 partial named object。创建期采用 whole-value table→cell precedence：table 值只实体化到 normalized cell border 缺失的位置；任意非空 cell scalar/TRBL/named/none 完整阻断 table 值，未指定的 cell sides 仍是 canonical none 而不跨层继承。empty/all-undefined cell 值会继承 supplied table 值；empty/all-undefined table 值保持 omitted bytes。创建与编辑的 missing-side 语义有意不同：创建以 canonical 四条 direct noFill 为基线，tuple/named 未指定边保持 none；编辑器是 whole replacement，未指定边删除 direct element，`{}` / `undefined` 清除四边且不重新继承。所有 border/side/color 输入都 descriptor-safe、getter-free、ordinary/null-prototype-only 并立即深度脱离。OOXML 始终按 L/R/T/B 写出，四边之后才写 optional cell fill。getter 独立省略 malformed/unsupported side，不读取或近似 diagonal、其他 dash、gradient/pattern line、compound/cap/join/arrow/effect，也不计算 adjacent shared-edge 或 effective table style；这些状态在无关 mutation 中原样保留。对 table/cell explicit scalar/TRBL none、sRGB line、solid/dash、scalar zero width 和 cell whole-value override，原生创建与 PptxGenJS 4.0.1 的最终 snapshot、geometry、margins、fill 和 direct side state 对等。差异是 native empty border 表示无有效 layer，而 PptxGenJS `{}` 默认 `666666`/1pt/solid 且 cell `{}` 会阻断 table default；native omitted style 不写 direct dash，而 PptxGenJS 默认 direct solid；native scalar/TRBL zero 都保留 zero，而 PptxGenJS TRBL item `pt: 0` 变成 1pt；native tuple 必须 dense 且恰好四项，PptxGenJS runtime short tuple 会把 missing sides 实体化为 none；native 额外支持 named sides 和 theme color。adapter 只读取最终 L/R/T/B XML，不逆推输入层级。table-level getter/editor、diagonal/advanced line/transparency、shared-edge layout 与 layout recomputation 仍未支持。

Table-cell `fill` 只读写选中物理 cell 唯一 direct `tcPr` 下的 fill choice，以 `{ kind: 'none' }` 区分 direct `a:noFill`，以 `{ kind: 'solid', color, transparency? }` 表达 strict sRGB/theme solid fill 和可选 direct alpha。`AddTableOptions.fill` 与 `AddTableCellOptions.fill` 复用这个 descriptor-safe、getter-free、ordinary/null-prototype-only value，并立即与 caller 深度脱离。table fill 是 whole-value 创建默认：只实体化到 cell fill 为 omitted/`undefined` 的 physical cells，cell solid/none 完整覆盖；omitted/runtime-undefined table fill 保持原 bytes，empty fill object 非法。OOXML 固定按 margins attributes、optional anchor、optional direction、L/R/T/B borders、fill 输出。live `TableModel` 会立即返回 resolved detached `TableCell.fill`；创建后不保留 table default，因此 `setCellFill()` 的 replace/clear 只作用于零基 physical cell 的最终 direct state，`none` 写 direct noFill，`undefined` 删除 direct choice且不会重新继承。transparency 是 finite `0..100` 百分比，量化到 `0.001%`；omitted 不写 alpha，explicit zero 写完全不透明 alpha。getter 只接受同 prefix、无歧义的 direct noFill/solidFill，不把 border/text descendants 当成 cell fill，也不解析 effective table style；其他 fill 类型在无关 mutation 中原样保留。PptxGenJS 4.0.1 对 supported table solid fill 与 cell solid override 生成相同 final direct cell state，但会把 omitted 与 `type: 'none'` 都折叠为没有 direct fill，并把 explicit zero transparency 折叠为没有 alpha；native direct none 和 explicit zero 因此保留可见 direct-state 差异。runtime 越界值可生成非法 alpha，adapter 保留 XML 但 strict snapshot 返回 `undefined`。创建时 align/border/fill/fit/margin/textDirection/valign 之外的 cell options、table-level direction/fill/border getter/editor、diagonal/advanced border、gradient/pattern/picture/group fill 与 layout recomputation 仍未支持。

Table-cell `textFit` 复用 `none`、`shrink`、`resize`。原生 plain-text cell 可通过 `AddTableCellOptions.fit` 创建 direct fit：omitted、runtime `undefined` 与 `none` 生成 byte-identical self-closing `bodyPr` 且 immediate snapshot 都是 `undefined`；`shrink` / `resize` 分别写唯一 direct `normAutofit` / `spAutoFit` 并立即 snapshot。创建不写 `noAutofit`、不保留 table metadata、不计算最终 font scale，也不测量内容或重算 cell/table geometry；fit child 与 `tcPr@vert` direction 独立。Getter 只读取唯一 direct `txBody/bodyPr` 中唯一 supported fit child，既有 `noAutofit` 映射为 `none`，缺失或 malformed 状态返回 `undefined`。`setCellTextFit()` 以零基 physical row/cell index 编辑；shrink/resize 写上述 child，none/`undefined` 都移除 direct choice、不恢复创建输入且不新增 `noAutofit`，同模式赋值保留 PowerPoint 已计算的 scale metadata。Table-level fit creation/default/getter/editor 均未支持。PptxGenJS 4.0.1 的 `TableCellProps` / `TableProps` 没有 fit API，runtime 透传的 `fit`、`autoFit`、`shrinkText` 均被忽略并生成 fit-less `bodyPr`；native shrink/resize creation 是明确扩展，不作为 PptxGenJS parity 声明。

LibreOffice headless 可正确显示 table-cell `horz`、`vert` 和 `vert270`，但当前把 `wordArtVert` 显示为水平文字；同一 direct OOXML 的 PptxGenJS 4.0.1 对照文件表现完全一致，`tcPr@vert="wordArtVert"` 仍保持合法并可由 PowerPoint 等支持该模式的客户端读取。

文本框 `fit` 的 omitted/none 都不写 direct child，shrink 写 `bodyPr/a:normAutofit`，resize 写 `bodyPr/a:spAutoFit`。`shape.textFit` 也读取既有唯一 `a:noAutofit` 为 none；none 或 `undefined` 清除 direct fit choice，同模式 shrink/resize 赋值保留 PowerPoint 已计算的 `fontScale` / `lnSpcReduction`。PptxGenJS 对非法 outer 值和 run-level fit 静默忽略，本库严格拒绝非法原生输入；adapter 仍兼容 deprecated outer `shrinkText` / `autoFit` 输出。最终缩放比例可能要由 PowerPoint 在编辑文字或改变 shape 大小时动态重算。

文本语言通过 `AddTextOptions.lang` 提供 plain/rich 创建默认值，`RichTextRunStyle.lang` 可覆盖单个 run。省略时 run 与 `endParaRPr` 使用 `en-US`；显式 outer/run 语言在 run 上同时写 `altLang="en-US"`，`endParaRPr` 只跟随 outer 值。getter 只暴露非空 direct `rPr@lang`，不解析 `altLang` 或 master/layout 继承。原生 API 拒绝非 string、空 string 和非法 XML 控制字符，并安全转义 attribute metacharacters。PptxGenJS 4.0.1 的 falsy fallback 仍由 adapter 输入兼容；未转义字符串产生的无效 XML 不属于兼容承诺。

Rich run `transparency` 使用 `0..100` 百分比：0 完全不透明，100 完全透明，并量化到最近 `0.001%`（`alpha = Math.round((100 - transparency) * 1000)`）。省略字段不写 alpha，显式 0 写 direct `alpha val="100000"`，因此本库可区分 absence 与 explicit opaque；PptxGenJS 4.0.1 因 truthy 判断会把 0 与 omitted 都省略。没有显式 run color 时本库沿用默认 `schemeClr tx1`，PptxGenJS 则实体化 direct black，effective transparency 相同。该字段只控制文字主 `rPr/solidFill`，不影响 outline、glow、highlight、underline、shape 或 table-cell fill。getter 只接受唯一 direct solid fill、唯一合法 sRGB/theme color 和唯一严格 integer alpha，不解析继承或其他/mixed color transform。

Paragraph 左右边距分别通过 `AddTextOptions.paragraphMarginLeft` / `paragraphMarginRight` 提供 plain/rich 创建默认，`RichTextParagraph.marginLeft` / `marginRight` 使用 point 提供本地 number override，`false` 抑制默认或清除对应 direct `pPr@marL` / `marR`。getter 仅读取 `0..51206400` 的 direct integer EMU，不解析继承。普通段落缩进通过 `AddTextOptions.paragraphIndent` 提供创建默认，`RichTextParagraph.indent` 接受 `-4032..4032` point：正值是 first-line indent，负值是 hanging indent，`false` 抑制默认或清除 direct `pPr@indent`。新建普通段落保留 canonical direct zero；setter omission 清除已有 direct 值。getter 仅读取 `-51206400..51206400` 的 direct signed integer EMU。direct bullet/numbering/picture bullet 拥有 `marL` 与 `indent`，因此 getter 不重复暴露 `marginLeft` 或 ordinary `indent`，同 paragraph 的 numeric left margin/ordinary indent 与 active bullet 也在 mutation 前拒绝；`marR` 与列表缩进独立，可在普通、bullet 或 numbering paragraph 上读取和编辑。左右字段与 indent sign 始终映射物理 OOXML，不因 RTL 交换。PptxGenJS 4.0.1 没有普通 paragraph 左右边距或 first-line/hanging indent 公开选项；其普通 paragraph direct `marL="0"` / `indent="0"`、列表 `marL` / negative `indent` 和默认缺失 `marR` 的输出均可正常导入。

Presentation RTL 通过 `CreatePresentationOptions.rtlMode` 创建，并可由 `document.rtlMode` 读取、编辑和清除。getter 只读取 direct `p:presentation@rtl`，接受 `1/true/on` 与 `0/false/off`；未知或 descendant token 不会伪造全局状态。true/false 分别写 `1` / `0`，undefined 清除 direct attribute。PptxGenJS 4.0.1 的 true 与 truthy 非 boolean 都写 `1`，false/omitted 都省略；本库严格拒绝非 boolean，显式 false 写 `0`，effective behavior 相同且 direct intent 可逆。全局 RTL 不改写 paragraph `pPr@rtl`、`bodyPr@rtlCol`、default text style、alignment 或 run 顺序。

段落 RTL 通过 `AddTextOptions.rtlMode` 提供 plain/rich 创建默认值，`RichTextParagraph.rtl` 可逐段覆盖。true/false 分别写 direct `pPr@rtl="1"` / `"0"`；setter 中省略字段会清除 direct override。getter 只接受 `1/true/on` 与 `0/false/off`，未知 token 返回 `undefined` 并在无关编辑中原样保留。该能力不读取或修改 `bodyPr@rtlCol` 或 presentation-level RTL，也不自动交换 alignment。PptxGenJS 4.0.1 的 valid outer true 多段输出可直接导入；其 run-level `rtlMode` 会在同一 paragraph 中插入重复且位置非法的 `pPr`，因此本库不公开 run-level RTL。

原生创建会生成可重新打开和验证的 master/layout/theme 关系链，不通过 adapter，也不在运行时安装或调用 PptxGenJS。
