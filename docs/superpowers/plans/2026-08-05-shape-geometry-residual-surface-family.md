# Shape Geometry Residual 3 Surface Family Implementation Plan

> **For agentic workers:** Evidence agents write only to `/tmp`; the main agent
> owns repository edits, review, matrix generation, commit, and push.

**Goal:** Close three residual `ShapeProps` atoms without adding inert aliases.

**Architecture:** Add one PptxGenJS aggregate control, map legal shortcuts to
existing direct adjustment evidence, and classify inert align from runtime.

**Tech Stack:** TypeScript, Vitest, PptxGenJS 4.0.1, OOXML, and surface audit.

## Global Constraints

- Close exactly three atoms as two deliberate differences / one defect exclusion.
- Make no product, npm-smoke, or browser-smoke implementation changes.
- Reuse the latest adjustment artifact gates instead of rebuilding them.
- Review, commit, and push the family as one unit.

---

### Task 1: Runtime and native projection

- [x] Lock inert align and legal geometry shortcuts in one aggregate control.
- [x] Reuse strict ordered `ShapeAdjustment[]` lifecycle evidence.

### Task 2: Matrix and documentation

- [x] Register the exact three IDs and 0/2/1 status split.
- [x] Regenerate the audit to 1,751 classified and 23 unverified.
- [x] Update root, package, baseline, and progress documentation.

### Task 3: Verify and synchronize

- [x] Run focused, TypeScript, audit, deterministic-generation, and diff gates.
- [x] Require Agent A/B/C and main-agent review.
- [x] Commit, push, and verify local/remote divergence `0 0`.
